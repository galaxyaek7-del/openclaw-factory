# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## الهوية والرؤية

🏭 Galaxy Forge ليست مصنع كتب فقط - هي منصة إنتاج رقمي متعددة المنتجات، قابلة للتحول الفوري بين المسارات لضمان الاستمرارية.

### المسارات الستة المخطط لها

| # | المسار | المنصات |
|---|--------|---------|
| 1 | الكتب الرقمية (نشط الآن) | Amazon KDP |
| 2 | القوالب الجاهزة | Etsy, Gumroad |
| 3 | اللوحات الفنية الرقمية | Etsy, Society6, Redbubble |
| 4 | التطبيقات والأدوات الإلكترونية | App stores, Web |
| 5 | خدمات رجال الأعمال | اشتراكات VIP |
| 6 | منصة تسوّق دولي | سيارات، بضائع جملة |

### المبدأ المعماري - 3 طبقات

```
الطبقة 1 — العقل المشترك (Core)
  الوكلاء الستة + Groq + الأدوات
  لا تتغير.

الطبقة 2 — محرّكات المنتجات (Engines)
  book_engine  template_engine  art_engine
  app_engine   service_engine   trade_engine
  مستقلة — قابلة للإضافة/الحذف.

الطبقة 3 — قنوات النشر (Channels)
  kdp_channel  etsy_channel  gumroad_channel
  shopify_channel  direct_channel
  لو منصة أغلقت، نوقف قناتها فقط.
```

### القواعد الذهبية

- لا توسع بمنتج جديد قبل أول دولار من المنتج الحالي
- كل محرك جديد يجب أن يتبع نفس واجهة `book_engine` الموجود
- الأمان: كل شيء محلي، API keys في `.env` فقط، لا cloud
- المحتوى: كل وكيل يجب أن يكون قابل للتحول بسهولة بين المسارات

### 💰 الفلسفة الاستراتيجية — شركة استثمار رقمي (توجيه المؤسس، 2026-07-23)

Galaxy Forge ليست شركة برمجيات — هي **شركة استثمار رقمي**. الكتب، القوالب، الأدوات، الكورسات هي أدوات استثمار، لا غاية بحد ذاتها. السؤال الدائم قبل أي بناء: "هل هذا يزيد القيمة طويلة المدى للشركة؟" — وليس "ماذا يمكننا أن نبني؟" (راجع `value_engine.py`، ADR-102 — الأداة الحقيقية التي تُجيب على هذا السؤال فعلياً لكل فرصة مقبولة اليوم).

**مبدأ الحياد التقني:** لا ولاء لأي موديل ذكاء اصطناعي أو شركة أو منصة بعينها (Claude, GPT, Gemini, DeepSeek, Qwen, Kimi, Doubao, نماذج مفتوحة المصدر — جميعها أدوات إنتاج مرشَّحة). معيار الاختيار الوحيد: نتائج أفضل، جودة أفضل، سرعة أفضل، موثوقية أفضل، اقتصاديات أفضل، نتائج أفضل للعميل. الأداة الحقيقية لهذا موجودة بالفعل: `ai_capability/registry.py` (Technology Investment Council) — يُسجِّل بيانات حقيقية لكل مزوّد فعلاً تم استدعاؤه (Groq اليوم)، ويُبقي كل مزوّد آخر (بما فيهم Kimi/Doubao المُضافان 2026-07-23) بحالة DISCOVERY صريحة حتى تتوفر بيانات اعتماد حقيقية واستدعاء حقيقي فعلي — لا مقارنة أداء مُختلَقة أبداً.

**التوسّع العالمي / برنامج الصين الاستراتيجي — مؤجَّل بوعي، ليس متجاهَلاً:** التوجيه نفسه (2026-07-23) طلب معاملة كل دولة كفرصة استثمار مستقلة (أمريكا، الصين، اليابان، كوريا الجنوبية، ألمانيا، بريطانيا، الهند، جنوب شرق آسيا، الشرق الأوسط) و"شعبة استخبارات صين" دائمة. بحث حقيقي (2026-07-23) وجد صفر موصّلات بيانات محلية حقيقية لأي من هذه الأسواق (لا Baidu/WeChat/Xiaohongshu/Douyin، ولا أي مصدر محلي آخر لبقية الدول) — المصدرين الخارجيين الوحيدين الحقيقيين في هذا المصنع (Hacker News + GitHub) عالميان/إنجليزيان، لا يمثّلان استخبارات سوق محلية حقيقية. هذا يتعارض أيضاً مباشرة مع **القاعدة الذهبية أعلاه** ("لا توسع بمنتج جديد قبل أول دولار من المنتج الحالي") — وصفر مبيعات حقيقية مكتملة موجودة على أي قناة حتى الآن. **القرار (تأكيد المؤسس، 2026-07-23):** توثيق الفلسفة الآن (هذا القسم)، لا بناء وحدات استخبارات سوق قُطرية بعد. يُعاد النظر عند: (أ) أول دولار حقيقي فعلي، أو (ب) توفّر موصّل بيانات محلي حقيقي فعلي — أيهما أسبق.

### ⚠️ قبل اقتراح أي بنية أمنية أو فصل حسابات

كل أداة/منصة في Galaxy Forge (GitHub، KDP، Gumroad، مستقبلاً Payhip/Etsy/Canva/Pinterest، Groq، Anthropic، n8n) مربوطة اليوم بحساب Google شخصي واحد، بلا استثناء. هذا قرار واعٍ (بساطة + تكلفة صفر لمرحلة مبكرة)، لا سهواً — راجع `OpenClaw_Brain/00_Governance/IDENTITY_ARCHITECTURE.md` والمخاطر الصريحة فيه، و`ADR-014` لمحفّزات إعادة النظر، **قبل** اقتراح أي تجزئة حسابات أو بنية أمنية جديدة.

---

## Running the project

```powershell
# Install Node dependencies (first time)
npm install

# Install Python dependencies (first time)
pip install reportlab

# Start the server
node server.js
# Runs at http://localhost:3000 (or PORT env var)

# Start it supervised instead (Enterprise Upgrade Roadmap Phase 1.1,
# 2026-07-23) — auto-restarts on a real crash, gives up and alerts via
# Telegram after too many restarts in a short window instead of looping
# forever. Fully opt-in: the manual `node server.js` above still works
# exactly as before, nothing about it changed.
node scripts/supervisor.js
```

There is no build step, test suite, or linter configured.

## Architecture

**Galaxy Forge** is an Arabic-language (RTL) dashboard for a self-publishing business selling digital products on KDP, Etsy, and Gumroad.

### Stack

| Layer | Technology |
|---|---|
| Frontend | Single-file vanilla HTML/CSS/JS (`index.html`), Arabic RTL, no framework |
| Backend | Express 5 (`server.js`), port 3000 |
| AI | Groq SDK — `llama-3.1-8b-instant` via `POST /chat` and `POST /api/agent/:name` |
| PDF generation | Python + reportlab (`book_generator.py`) |
| Data | `finance_data.json` (flat JSON, written directly by server) |

### Key flow: book generation

The browser calls `POST /generate-book` → `server.js` spawns a Python subprocess (`book_generator.py --json`) → passes parameters via `stdin` as JSON → reads JSON result from `stdout`. The PDF is written to the project root directory with a filename derived from the book title.

`book_generator.py` is standalone and can be run directly:
```bash
python book_generator.py          # generates demo_cookbook.pdf
```
When invoked with `--json`, it reads a JSON object from stdin:
```json
{"title":"...", "subtitle":"...", "type":"journal", "theme":"blue", "pages":120, "author":"...", "output":"filename.pdf"}
```
Supported `type` values: `journal`, `planner`, `habit`, `gratitude`, `fitness`, `tracker`, `healthy_eating`, `budget`, `mindfulness`, `cookbook`.

### Environment variables (`.env`)

```
GROQ_KEY=<your Groq API key>
PORT=3000
N8N_PRODUCTION_WEBHOOK_URL=<optional — n8n webhook URL for production-completion notifications, unset by default>
```

`N8N_PRODUCTION_WEBHOOK_URL` gates `server.js`'s `notifyN8nProductionEvent()` (n8n Integration Gap fix): when the `start-production-pipeline` Mission Control action completes at least one real dossier, it fire-and-forget `POST`s a plain projection of that dossier to this URL if set. Unset by default — a real production run must never fail or block on n8n being unreachable or unwired. See `n8n_workflows/README.md`'s "n8n Integration Gap audit" section for the full workflow map, what's proven live vs. still blocked on manual n8n activation (`BLOCKERS.md` #1), and the exact steps to wire this up.

(This file's own env-var list above predates several since-added optional vars from later work sessions — e.g. `MISSION_CONTROL_PASSWORD`, `FACTORY_LIVE_PUBLISH`, `GUMROAD_ACCESS_TOKEN`, `INTERNAL_SERVICE_TOKEN` (2026-07-23, a shared secret real internal automation — `factory_loop.js`, the `01_Market_Scout` n8n workflow — sends as `X-Internal-Token` to reach the handful of routes that need real automated access with no browser session available; see `server.js`'s `requireMissionControlOrInternalToken`) — each documented at its own point of use in `server.js` rather than cross-referenced here; not fully re-catalogued in this edit, which is scoped to the n8n gap fix only.)

### Agent endpoints

`POST /api/agent/:name` — **live Groq calls**, not stubs. **Gated behind `requireMissionControlAuth` since the Enterprise Security & Cyber Defense Mission's Phase 1 Security Audit (2026-07-23, finding 2.1)** — this route previously had zero authentication, meaning anyone who could reach it could spend the founder's real Groq API budget with an arbitrary `message` and no rate limit; that is now fixed. A logged-in Mission Control session (via `mission_control_login.html`) is required before `index.html`'s own buttons that reach this route will work — the same real prerequisite `/finance/add` already had before this fix, now extended here and to most other previously-open routes (see `ENTERPRISE_UPGRADE_ROADMAP.md`'s finding 2.1 for the full route list and which ones instead accept `INTERNAL_SERVICE_TOKEN` for real internal automation callers that have no browser session). Each of the six agents has a dedicated `system` prompt defined in the `AGENT_PROMPTS` object in `server.js` (lines 127–190):

| Agent | Role |
|---|---|
| `scout` | Market analysis — trending niches, pricing, competition level |
| `builder` | Content generation — book structure, sample pages |
| `design` | Cover & interior design ideas for 6×9 inch books |
| `qa` | Quality checklist, common KDP/Etsy rejection reasons |
| `publisher` | SEO-optimised titles, descriptions, keywords, Amazon categories |
| `finance` | Pricing strategy, profit margins per platform, break-even |

Each agent accepts an optional `message` in the request body; if omitted, a default `trigger` prompt fires automatically. `index.html` only wires UI buttons for `builder`/`design`/`publisher`/`qa` — the bare `scout` and `finance` agents above are live and reachable via `POST /api/agent/scout` / `POST /api/agent/finance`, but have no UI trigger (the fuller `/api/scout/run` pipeline below is what the UI's Scout button actually calls).

There is also a separate, more complete Scout pipeline: `POST /api/scout/run` runs a real end-to-end chain — Groq market brief → pricing (`getButterPrice()` in `factory_loop.js`) → niche safety filter (`safety_filter.py`) → `book_generator.py` → auto-distribution (`autoDistributeScoutBook()` → `distributor.py`). This is distinct from the simple `POST /api/agent/scout` text-analysis call above and is what the dashboard's Scout button triggers.

`POST /chat` (`server.js:1768`) — a raw Groq passthrough (any `message` in the request body goes straight to `llama-3.1-8b-instant`). Confirmed via full-repo audit (STRUCTURAL_DIAGNOSIS.md disease #8) to have **zero callers**: not `index.html`, not `factory_loop.js`, not any test, not any other doc. Predates the six-agent `POST /api/agent/:name` system above and was superseded by it in every practical sense. **Gated behind `requireMissionControlAuth` since the Full Factory Integrity Audit (2026-07-22)** — this line previously described it as unauthenticated; that was fixed in code but never updated here until now. Still zero real callers, so still a candidate for removal rather than a route to build on.

### Finance data

Finance is persisted to `finance_data.json` in the project root. The server reads/writes this file synchronously. Structure:
```json
{"sales": [...], "totalKDP": 0, "totalEtsy": 0, "totalGumroad": 0}
```
`DELETE /finance/delete/:id` (`server.js:558`) exists alongside `POST /finance/add` but has no UI button wired to it in `index.html` — a half-finished CRUD pair, not dead code (low risk: local JSON mutation only, id-validated).

### Executive Dashboard

`GET /api/dashboard` (`server.js`) and the static page `dashboard.html` (served automatically by `express.static`, plain UTF-8 unlike `index.html`) aggregate real, already-existing signals into one view: `computeHealthStatus()`, `self_awareness.assessSelfAwareness()`, and `lib/dashboard_data.js`'s pure file reads over `golden_opportunities.json`, `finance_data.json`, `market_hunter_runs.log`, `pending_review/`, `tier1_intake/candidates/`, `NEEDS_ATTENTION.md`, `NEEDS_REVIEW.md`. It computes nothing new and fabricates no metric — a section with no data yet reports that honestly (e.g. `tier1_discovery.accepted` is 0 today, matching `ADR-035`/`ADR-036`'s real finding, not an invented KPI). `lib/dashboard_data.js` has no Express/`child_process` dependency, so it's unit-tested in isolation (`tests/test_dashboard_data.js`) without needing a live server.

`GET /health` (`computeHealthStatus()`, `server.js`) gained real infrastructure checks (Enterprise Infrastructure & HA Mission, 2026-07-23, finding 4.7): memory, CPU (honestly reports `os.loadavg()` as unavailable on Windows rather than faking a number), disk space (Windows-only today — no POSIX deployment exists yet to build a cross-platform check against), real network reachability to this factory's actual external dependencies (Groq, Telegram if configured, GitHub), and `storage_integrity` — real per-line JSON/JSONL validation of `data/decisions.jsonl`/`finance_data.json`/`data/factory_state.json`, the honest analog to "database health" for an architecture that has none. `database`/`queue_system`/`worker_pool` are reported as explicit `not_applicable` (never a fabricated green check) — this factory has no database, no message queue, and no worker pool. Logic lives in `lib/health_checks.js`, same no-Express-dependency isolation-testing pattern as `lib/dashboard_data.js` (`tests/test_health_checks.js`).

### Customer Platform

`customer_site/` is Galaxy Forge's real public customer-facing platform, served at `/site/` — a separate, English, dark-themed surface from the founder-facing Arabic Mission Control. Built across Phase 1/2 Round 1 (ADR-129/130, 2026-07-25) and Rounds 1-6 (ADR-131, 2026-07-29). Real end-to-end pipeline: intake → Qualification/Pricing (reuses `decision_engine.evaluate_and_decide`/`profit_oracle.butter_price`, the same real engines every internal opportunity uses) → real contract generation + typed-name e-signature acceptance → real Paddle checkout attempt → real payment-completion check + invoice → honest Production/QA/Delivery routing → real download center → real customer reviews.

- **Accounts**: `lib/customer_auth.js` (scrypt password hashing, its own signed-cookie session — deliberately separate from Mission Control's `mc_session`, zero new npm dependencies). `POST /api/customer/signup|login|logout`, `GET /api/customer/session`, `customer_site/login.html`. Guest (no-login) submission remains fully supported; a logged-in visitor's `account_id` is stamped onto their request for later history matching (by `account_id` OR email, reconciling pre-account guest requests).
- **The pipeline** (`customer_pipeline.py`, all commands spawned directly from `server.js`, not via `mission_control_api.py` — a different, customer-facing trust boundary): `advance_request` → `approve_request` (now requires `accepted_name`, a real contract e-signature via `contract_generator.py`) → `_attempt_payment_verification` → `check_payment_status`/`check_all_awaiting_payments` (real Paddle transaction confirmation + `invoice_generator.py`) → `_fulfill_paid_request` (honest routing: reuses an already-published `books/_generation_log.jsonl` artifact for a matched catalog product, else the side-state `PENDING_FOUNDER_FULFILLMENT` for a genuinely bespoke request, resolved via the founder's own `fulfill_manually()`) → `list_requests_for_account`/`submit_review`/`mark_followed_up`.
- **Real gap disclosed, not built**: customer-facing email/SMS notification is 0% built — the pull-based `customer_site/status.html`/`history.html` pages are the real substitute today. Every stage transition still notifies the founder via the existing Telegram channel.
- **Still genuinely blocked**: Paddle's account-onboarding gate (`transaction_checkout_not_enabled`) — every `PAID`-and-later code path is real but stays functionally unexercised against a live account until the founder clears it in `vendors.paddle.com` (`scripts/check_paddle_checkout_status.py` re-checks this live, never from cache).
- **Mission Control** gained a real executive cockpit for all of the above: `mission_control_executive_v1.html`'s card grid has panels for Customer Accounts, the Customer Pipeline, the Production/Fulfillment Queue, Invoices, and Reviews — all pure passthrough over the real state above, zero new dashboard file, zero fabricated metrics.

### Autonomous Company Evolution Engine

`evolution_queue.py` (new, ADR-133, 2026-07-29) is the real Observe → Think → Simulate → Decide → Execute → Learn → Protect → Dashboard pipeline the founder's "AUTONOMOUS COMPANY EVOLUTION ENGINE" directive asked for. **Execute is, by explicit founder decision, human-gated always — this must not be loosened without asking again.** No proposal, regardless of its computed risk tier, executes automatically: every real proposal from `tool_intelligence/proposals.py::list_proposals()` (6 real signal sources as of Global Trust & Resilience Layer Round 1, 2026-07-29 — bottlenecks, technical debt, capability gaps, a stuck-customer-funnel signal, a marketplace-publish-protection signal, and a health-degradation-trend signal) is intook, mechanically simulated (`affected_modules`/`rollback_complexity`/`sensitive_areas_touched` — a disclosed static evidence-text heuristic, not a real execution sandbox), and decided (real `policy_risk`/`duplicate_architecture_risk` flags, never an auto-approve/auto-reject) into a real state machine in `data/evolution_queue_state.json`: `PROPOSED → SIMULATED → AWAITING_FOUNDER_APPROVAL → APPROVED/REJECTED → IMPLEMENTED`.

`factory_loop.js`'s daily tick (`maybeGenerateDailyEvolutionQueueIntake()`) runs intake/simulate/decide once per calendar day — it stops at `AWAITING_FOUNDER_APPROVAL` and never calls approve/reject/mark-implemented. Those three are exclusively founder-triggered Mission Control actions (`POST /api/v1/actions/approve-evolution-proposal|reject-evolution-proposal|mark-evolution-proposal-implemented`, each requiring `{ proposal_id }`) — the one concrete code enforcement of "human-gated always," not just a policy note. `mark_implemented()` only closes the loop after a real, separately-reviewed Claude Code session has actually shipped an `APPROVED` proposal; there is no code path that writes production code from this system itself. Its `note` field is this factory's real "lessons learned" record (Decision Memory, Round 6, 2026-07-29) — not duplicated with a second field elsewhere.

**Outcome measurement + ranking (ADR-143, 2026-07-30)**: the founder's follow-up "AUTONOMOUS EVOLUTION ENGINE" directive asked this pipeline to "continuously measure whether every implemented evolution actually improved" revenue/reliability/scalability/automation/customer value/execution speed, and to rank proposals by strategic value/revenue impact/execution cost/long-term sustainability/risk/confidence. A research audit found ~90% of the directive already built the day before (this section, above) — the two genuinely new, previously-uncovered pieces were built: `mark_implemented()` now captures a real `baseline_snapshot` (revenue via `channels/ledger.py::revenue_trend()`, reliability via `health_trend.py`'s real `GET /health` history, customer value via `customer_pipeline.py::funnel_conversion_summary()`); `measure_outcome(proposal_id, min_days_elapsed=7)` compares a fresh snapshot against that baseline no sooner than 7 real elapsed days later, honestly reporting IMPROVED/DEGRADED/NO_CHANGE/NOT_ENOUGH_DATA — scalability/automation/execution speed report an explicit `NO_REAL_SIGNAL`, never a fabricated number, since no real per-proposal signal for any of them exists in this factory. Each real call appends to the record's own `outcome_measurements` list — the actual trend, not a one-shot check. `run_daily_outcome_measurement_cycle()` (wired into `factory_loop.js`'s `maybeMeasureEvolutionOutcomes()`, same once-per-calendar-day gate) is the only automatic path; it never approves/rejects/marks-implemented. `rank_proposal()` is a disclosed heuristic over fields the record already has (`simulation.rollback_complexity`, `decision_flags`, keyword match on proposal text) — `strategic_value` is honestly `not_computed`, since `strategic_intelligence_core.strategic_score()` is per-niche and most evolution proposals are company-wide; never a fabricated numeric priority score. New department/product/AI-model proposal categories the directive also named were deliberately not built — no real underlying signal exists for any of them yet, same disclosed-gap discipline as `capital_allocation_engine.py`'s own "Reduce vs. Pause" gap. A real bug was found and fixed during this round: `_record_history()`'s own detail-logging on each measurement was silently resetting the elapsed-time clock used to gate the next measurement — fixed by storing `implemented_at` once, explicitly, rather than re-deriving it from a mutating `stage_history` list; covered by a named regression test. Mission Control gained `evolution-measured-outcomes` (`SERVICE_REGISTRY`, read-only) alongside the existing `evolution-queue` panel (which now also carries each entry's `ranking`).

New Observe signals live in `customer_pipeline.py`: `funnel_conversion_summary()`, `delivery_delay_summary()`, and a stuck-`PROPOSED` ("abandoned at proposal") check alongside the existing stuck-`NEW` one — all honestly empty/Unknown today (zero real customer traffic). `knowledge_graph/build.py` gained a `Proposal` node type (mechanical parse of `data/evolution_queue_state.json`, same non-semantic discipline as its `Lesson`/`ADR` nodes) — the real Learning History. Mission Control's `evolution-queue` panel is read-only passthrough over `evolution_queue.py::list_evolution_queue()`; `founder_console.py` surfaces the real `AWAITING_FOUNDER_APPROVAL` count alongside its existing DEFERRED-decisions count.

### Global Commercial Hardening — Marketplace Publish Protection Layer

`channels/publish_protection.py` (new, ADR-134, 2026-07-29): a real, disk-persisted (atomic write, `data/publish_protection_state.json`) pre-publish gate — per-arm daily/hourly caps, a real time-based cooldown after repeated failures, minimum publish spacing, a disclosed `risk_score` heuristic, and a global emergency stop. `PLATFORM_PROFILES` names Amazon KDP, Etsy, Gumroad, Payhip, Shopify, and AliExpress even though only Gumroad/Etsy/Payhip/Paddle have a real registered `channels/base_arm.py` arm today (KDP/Shopify/AliExpress are genuinely greenfield) — a conservative `_default` profile covers any arm name generically.

`distributor.py::distribute()` calls `check_publish_allowed()` immediately before every real (non-dry-run) `arm.publish()` — a blocked arm never reaches the real platform call. Dry runs are exempt entirely (no real-world effect to protect against). `channels/ledger.py::record_publish_attempt()` gained optional `risk_score`/`protection_decision` kwargs, omitted from the event when not given — every pre-existing caller's event shape is unchanged. Mission Control exposes a read-only `publish-protection-status` panel plus founder-only `publish-emergency-stop`/`publish-emergency-resume` sync actions (`POST /api/v1/actions/...`, mirroring `pause-production`/`resume-production`'s exact shape) — never fired automatically. `founder_console.py` surfaces an active emergency stop as a real pending-decision item; `executive_score.py` gained a `publishing_safety` sub-score; `tool_intelligence/proposals.py` gained a dynamic generator, `_marketplace_protection_proposal()` (see Autonomous Company Evolution Engine above).

`executive_quality_gate.py::check_content_neutrality_risk()` (Executive Safety Principles, same directive) is a new named hard-reject criterion — a real phrase-list content scan for the founder's explicit "never evolve toward: political activity, religious discussions, ethnic classification, sensitive personal profiling, illegal content, hate content, manipulation, privacy violations, government interference" list, permanently recorded in `OpenClaw_Brain/00_Governance/EXECUTIVE_SAFETY_PRINCIPLES.md`.

**Explicitly deferred, not built**: a Global Partner/Affiliate/Commission Engine and broader Commercial Maturity (subscriptions, B2B/enterprise licensing) — real research this session found zero scaffolding exists and, more fundamentally, no payables/liability ledger exists anywhere in this factory's financial model (`finance_data.json`/`channels/ledger.py` record revenue only). Building a commission engine without that primitive first would risk fabricated financial bookkeeping. See ADR-134 for the full design sketch reserved for that future round.

### Global Trust & Resilience Layer

Global Trust & Resilience Layer (ADR-135, 2026-07-29) closed a handful of real, scoped gaps found by research against this directive's 10 priorities — most of it was already built by the two directives above.

- **Self-Diagnosis**: `lib/health_trend.js` (real recording — `factory_loop.js`'s tick already fetches a real `GET /health` status every cycle; this appends it to `data/health_snapshots.jsonl`) + `health_trend.py` (the Python-side mirror, same per-language-reimplementation precedent as `executive_score.py`'s `_dual_inspection_pass_rate()`). `detect_health_degradation()` is a real, disclosed mechanical trend heuristic over real recorded history — never a fabricated prediction.
- **Safe Mode**: `safe_mode.py` (new) generalizes the existing per-arm circuit breaker to three real named subsystems — `ai_generation`/`market_intelligence` (their own independent flag) and `marketplace_publishing` (deliberately no independent flag — reads `channels/publish_protection.py`'s own global emergency stop directly, never duplicated). Mission Control: `safe-mode-status` panel, `mark-subsystem-unstable`/`clear-subsystem-unstable` actions.
- **Global Policy Engine**: `executive_quality_gate.py` gained `check_constitution_alignment()` (a real, informational-only citation of which `CONSTITUTION.md` principles a decision's already-running checks actually satisfy — never a new hard gate) and `check_platform_tos_awareness()` (honestly `UNKNOWN` always today — zero platform Terms of Service text is parsed anywhere in this factory; a disclosed gap, never a fabricated PASS).
- **Founder Protection**: the founder's explicit resolution to a real conflict — proven arms (`gumroad`/`etsy`/`payhip`/`paddle`) keep their existing autonomous publishing unchanged; `channels/publish_protection.py` now also blocks a genuinely new arm's first-ever real publish, and any publish whose `risk_score` crosses a real high-risk threshold, until a real founder-only `approve-first-publish`/`approve-elevated-risk-publish` Mission Control action clears it (the latter single-use, consumed by the very next real attempt).
- **Human Trust**: `profit_oracle.py::opportunity_score()` gained a parallel `components_basis` dict tagging each component `"real"` (a live signal actually fed it) or `"estimated"` (a fixed per-tier constant or keyword heuristic) — read-only metadata, the real `components`/`accepted` values are unchanged.
- **Decision Memory**: `decision_engine/types.py::Decision` gained optional `alternatives_rejected`/`expected_outcome` fields. The real "actual outcome" pipeline (`decision_engine/feedback.py::sync_outcomes()` → `data/decision_outcomes.jsonl`) already existed and was already wired — the gap was that it was never linked into company memory; `knowledge_graph/build.py` now adds a real `Outcome` node with a `resulted_in` edge back to its `Decision` node, mechanically joined by `decision_id`, only for a real matched outcome.
- **Continuous Improvement**: `evolution_engine.py::build_evolution_report()` gained `customer_success_bottleneck`, reusing (not duplicating) `tool_intelligence.proposals`' own real customer-funnel signal, framed explicitly around customer success.
- **Reputation Protection**: confirmed no fabrication path exists anywhere in this factory (reviews/finance/analytics are real-input-only) — `channels/publish_protection.py`'s existing rate/cooldown state is documented as the real anti-spam/anti-aggressive-publishing control, not duplicated.
- **Resilience Tests**: `tests/test_resilience.py` (new) — a real, mocked fault-injection suite (never a live chaos system against real production data) proving cross-cutting recovery properties across `distributor.py`, `factory_state.py`, `channels/publish_protection.py`, `safe_mode.py`, and `recovery/startup_check.py` that no single module's own unit tests already covered.

### Continuous Trust & Resilience Monitoring

`resilience_monitor.py` (new, ADR-136, 2026-07-29) is the real Monitor + Classify + Learn aggregator the founder's follow-up directive asked for — "transform the [Global Trust & Resilience] layer into a permanent monitoring system," not more isolated fixes. Every signal it uses already existed (`safe_mode.py`, `channels/publish_protection.py`, `customer_pipeline.py`'s `needs_attention`, `ai_doctor.py`'s dependency-pinning, `health_trend.py`); this module's only genuinely new pieces are a shared 4-tier severity vocabulary (`informational`/`warning`/`critical`/`emergency` — no existing signal used this exact scale) and a real incident-memory record.

`assess_resilience()` classifies every monitored area and computes a transparent `resilience_score` — the exact `executive_score.py` precedent (an average of only the real, data-available findings, informational only, disclosing how many were excluded and why, never gating anything). `record_incident()`/`list_incidents()` (`data/incidents.jsonl`, new) open a real incident only on a genuine new critical/emergency finding (deduped — an already-open incident is never re-recorded on a later tick) and close it with a real "resolved" event once the condition clears; every Learn field (`root_cause`, `prevention_rule`, `detection_rule`, `improvement_proposal_id`, `rollback_guidance`) is either a real evidence-cited template or honestly `None`/`"Unknown"` — `improvement_proposal_id` specifically is only ever cited when that id is **actually present** in `tool_intelligence.proposals.list_proposals()`'s real, live output, never a static guess.

Mission Control: `resilience-status` (merges the Python signals with `computeHealthStatus()`'s `storage_integrity` and `lib/dashboard_data.js`'s reviews/support-tickets, same merge pattern as `executive-score`) and `resilience-incidents`, both read-only. `factory_loop.js`'s `runResilienceMonitorTick()` runs **every tick** (not daily-gated, the honest definition of "real-time" a scheduler-less factory can offer) but adds zero new auto-executing code path — it never calls `trigger_emergency_stop()`/`mark_subsystem_unstable()` itself; every irreversible/high-impact lever stays exactly as founder-gated as ADR-134/135 left it.

### Strategic Intelligence Core

`strategic_intelligence_core.py` (new, ADR-137, 2026-07-29) is the founder's named "Executive Brain" directive — a top-level decision layer answering 10 strategic questions, a named 11-dimension "Strategic Score," a once-per-cycle "Executive Brief," and multi-year thinking. A research audit found most of this already built the previous day under different names (`ceo_decision_center.py::answer_ceo_questions()`/`ceo_dashboard()`, `value_engine.py`'s 17-dimension `dimensions` dict, `scheduler.py::decide_next_actions()`'s accelerate/pause buckets, `evolution_engine.py`'s bottlenecks) — this module's real job is three genuinely new, narrow pieces, each a citation layer, never a second judgment engine.

`evaluate_strategic_horizons()` — 30d/90d/1y/3y/10y, each honestly `"NOT ENOUGH EVIDENCE"` today: the short horizons because `growth_engine.growth_forecast()` itself has no real windowed-comparison algorithm yet even at `REAL` maturity, the long horizons because this factory (founded 2026-07-05) cannot possibly have 2 real comparable multi-year sales windows yet — never a projected guess. `strategic_score(niche)` — the 11 named dimensions as `{value, source, reason}`, via `_extract_signal()`, a real normalizer over the genuinely different real shapes this factory's signals already arrive in (`{"value":...}`, `{"level":...}`, `{"favorability_score":...}`, `{"score":...}`, bare numbers, or `{"answer":"Unknown",...}`); Competition/Demand (`pain_level`)/Difficulty are sourced directly from `opportunity_pipeline.annotate_decision()` since `value_engine`'s own `dimensions` dict doesn't re-expose them. `build_executive_brief()` — the one real aggregator this factory never had, its 9 named fields each citing `resilience_monitor.assess_resilience()`, `ceo_decision_center.ceo_dashboard()`, `evolution_engine.build_evolution_report()`, `scheduler.decide_next_actions()`, `founder_console.build_founder_queue_partial()`, and this module's own horizon/customer-cost-trend signals for `research_needed` (honestly empty when every signal resolves).

`customer_pipeline.py::customer_problem_cost_trend()` (new Observe signal, same "recent-7d vs. trailing-daily-average" technique as `channels/ledger.py::revenue_trend()`) feeds `research_needed` alongside the horizons. Mission Control's `executive-brief` section returns `{report, markdown}` (same shape as `evolution_report`/`ai_doctor`/`department_health` — one dispatch serves both the live panel and `factory_loop.js`'s `maybeGenerateDailyExecutiveBrief()`, same once-per-calendar-day gate as those three). Every function here is read-only/recommend-only; Constitution-first and no-autonomous-high-risk-decisions were already real everywhere an irreversible action exists in this factory before this round began.

### Galaxy Council

`galaxy_council.py` (new, ADR-138, 2026-07-29) is the founder's "GALAXY COUNCIL — THE UNIFIED EXECUTIVE BOARD" directive: 10 named members, each producing `{opinion, confidence, evidence, risk, recommendation, founder_approval_required}`, honest disagreement never fabricated into a fake consensus. A research audit found `executive_board.py` (2026-07-22) already solves this exact class of problem once, under different member names (10 C-suite roles with real tally/consensus/disagreement/track-record-review) — the Council's real job is 9 new per-domain citation functions (Strategic/Market/Production/Customer/Financial/Security/Resilience/Innovation/Executive-Memory; Mission Control is the display venue, deliberately given no fabricated 10th opinion) plus a genuinely new N-way disagreement algorithm and a 3-way learning join, reusing `executive_board.py`'s tally/consensus pattern as the literal template rather than re-solving it. Three distinct rosters now exist (Constitution's 12 Councils, Executive Board's 10 C-suite roles, Galaxy Council's 10 named members) — deliberately not merged; see ADR-138 and the cross-reference note in `OpenClaw_Brain/06_Councils/README.md`.

Each member tags a `stance` (`proceed`/`hold`/`stop`/`no_data`/`informational_context`) only where its own real source already emits something genuinely comparable to a go/no-go signal (Financial's `accepted`, Production's `passed`, Market's critical/high alerts, Strategic's `at_risk` flag) — members whose sources are informational-only by their own docstrings (Security/Resilience/Innovation/Customer, all structurally company-wide-only — no niche field exists anywhere in their real data models) are never coerced onto that axis. `convene_council(niche)` calls all 9 every time (company-wide members tagged `scope: "company_wide"`, never silently skipped); `disagreement_detected` is true iff 2+ real-stance members genuinely disagree, generalizing `decision_engine.engine`'s real 2-signal-must-agree rule to N members — when they agree, `council_recommendation`/`council_confidence` cite that real agreement; when they disagree, both are honestly `"SPLIT — see disagreeing_members"`, never blended.

`data/council_recommendations.jsonl` (new, append-only) + `record_council_recommendation()` (explicit, human-triggered only) + `council_learning_summary()` close the Learning loop: a real 3-way join (Council Recommendation → the real Founder Decision that followed, via `decision_engine.store` → the real eventual Outcome) with the same `matched`/`diverged`/`pending`/`too_soon_to_tell` non-forced-verdict discipline as `executive_board.review_board_track_record()` — honestly `NOT ENOUGH EVIDENCE` until real triples accumulate, never backfilled. Mission Control: `convene-galaxy-council`/`record-council-recommendation` in `server.js`'s `ACTION_REGISTRY` (the correct precedent — `convene-executive-board`'s async-job/15-minute-timeout shape, not the company-wide `SERVICE_REGISTRY` 30-second-default shape, since a per-niche convening chains several real sub-calls), a niche-input button in `mission_control.html`'s Opportunities tab, and a no-input `galaxy-council-learning` panel in both `SERVICE_REGISTRY` and `mission_control_executive_v1.html`.

### Capital Allocation Engine

`capital_allocation_engine.py` (new, ADR-139, 2026-07-29) is the founder's "CAPITAL ALLOCATION ENGINE" directive: a continuously-updated 14-dimension "Investment Score" per initiative, dynamic resource recommendations, real opportunity-cost detection, and sunk-cost/shiny-object/founder-bias protection. A research audit found 7 of the 14 named dimensions directly duplicate `strategic_intelligence_core.py::strategic_score()`'s own 11 dimensions, built the immediately preceding round — `investment_score(niche)` delegates verbatim to `strategic_score()` for those 7 (never a second, competing citation function) and adds real citations only for the genuinely new 7 (Expected Revenue, Customer Impact, Market Defensibility, Engineering Cost, Maintenance Cost, Knowledge Reuse, Brand Value), sourced from `value_engine.compute_value_profile()`'s own dimensions/board_summary fields. 4 of those 7 turned out to already be honestly `Unknown` inside `value_engine.py` itself (`_NO_REAL_SOURCE_DIMENSIONS` + `estimated_maintenance_cost`) — no new code needed, only honest citation.

`opportunity_cost()` — the real, genuinely new gap: for every real ACCEPTED opportunity in `scheduler.py`'s own `wait`/`stop`/`cancel` buckets, cites which `run_now`/`accelerate` opportunities rank higher by the exact same real Priority Score order `value_engine.build_value_engine_report()` already computed — "resources are effectively going to X instead of Y," never invented. `stuck_without_production()` — "Projects Consuming Resources Without Results": a real ACCEPTED decision with zero real production attempt (`value_engine.classify_lifecycle_stage()`'s own real `prototype` stage) across a real, disclosed elapsed-time threshold, modeled on `customer_pipeline.py`'s existing stuck-request pattern. `build_capital_allocation_dashboard()` — the one real aggregator (Top ROI Initiatives, Projects Losing Value, Projects Consuming Resources Without Results, Resource Distribution, Expected Portfolio Return, opportunity-cost pairings), computing the real portfolio/scheduling scan exactly once and threading it through every field that needs it (measured live ~22-25s, given a disclosed 90s Mission Control timeout, same outlier class as `executive-brief`).

Sunk-cost-fallacy protection is documentation, not code: neither `profit_oracle.opportunity_score()` nor `value_engine.compute_value_profile()` accepts any cumulative-past-spend parameter anywhere in their real signatures — a decision here is architecturally incapable of being anchored to money already spent. The directive's "Reduce" vs. "Pause" and "Accelerate immediately" vs. "Increase" distinctions have no real signal anywhere today — disclosed as an honest gap in ADR-139, never fabricated. Mission Control: `investment-score` (per-niche, `ACTION_REGISTRY`, 15-minute timeout, same precedent as `convene-galaxy-council`) + a button in `mission_control.html`'s Opportunities tab; `capital-allocation-dashboard` (no-input, `SERVICE_REGISTRY` + `mission_control_executive_v1.html` panel).

### Global Opportunity Exchange (GOX)

`global_opportunity_exchange.py` (new, ADR-140, 2026-07-29) is the founder's "GLOBAL OPPORTUNITY EXCHANGE" directive: every monitored marketplace as a live asset, portfolio-value optimization, and concentration-risk detection against 4 named thresholds (>40% one platform, >30% one product family, >25% one country, >20% one AI provider). A research audit found this directive collides directly with facts CLAUDE.md itself already documents: only 4 of the 15 named marketplaces (`etsy`/`gumroad`/`payhip`/`paddle` — 2 of which the directive itself never named) have a real registered channel arm, 0 real sale events exist, and country-level intelligence is the founder's own confirmed 2026-07-23 deferral (see "التوسّع العالمي" above) — this module is, by design, the most DISCOVERY-heavy of this session's CEO-directive modules, and that emptiness is the correct disclosure of this factory's real current state, never backfilled to look more built-out.

`marketplace_catalog()` reuses `growth_engine.py::premium_product_catalog_status()`'s exact "map N founder-named categories onto real adapters, honestly DISCOVERY for the rest" pattern. `product_family_distribution()` (new, mirrors `value_engine.py::_score_synergy_and_bundle()`'s real `Counter`-over-ACCEPTED-decisions technique) found a genuine, real 100% concentration in `automation_systems` across this factory's 3 real ACCEPTED decisions today — a real finding, not invented. `revenue_distribution()` (new, groups real `channels/ledger.py` sale events by `platform`) is honestly `NOT ENOUGH EVIDENCE` — 0 real sales exist yet. `ai_provider_concentration()` cites `ai_capability/registry.py`'s real Groq stats — trivially ~100% since no second real provider has ever been called, explicitly disclosed as "not yet a real diversification signal." `country_dependency_note()` is not a computation — a permanent, real citation of the founder's own 2026-07-23 decision, always the same structural DISCOVERY. `market_health()` cites `channels/publish_protection.py`'s already-real per-arm state as the real "Market Health"/"Market Saturation" signal for marketplaces-as-distribution-channels (a distinct concept from `competitor_discovery.py`'s niche-saturation, confirmed not conflated anywhere). `build_global_opportunity_exchange_dashboard()` computes the real portfolio scan exactly once and produces real, mechanical `diversification_recommendations` — one line per concentration check that actually crosses its real threshold, never padded. Mission Control: `global-opportunity-exchange` (no-input, `SERVICE_REGISTRY`, 60s timeout — measured live ~11-15s — + `mission_control_executive_v1.html` panel); no niche-scoped action, since GOX is portfolio-wide by its own mandate.

### Autonomous Business Builder (ABB)

`autonomous_business_builder.py` (new, ADR-141, 2026-07-29) is the founder's "AUTONOMOUS BUSINESS BUILDER" directive: a 12-section Business Blueprint + 8 named estimates + a phased execution roadmap per Founder-approved opportunity. A research audit found near-total overlap with two already-real, already-wired modules built before this session began: `business_dossier.py` (8-section deterministic template, already called inside `opportunity_pipeline.annotate_decision()` for every real opportunity, already rendered live in `mission_control.html`) and `production_blueprint.py` (a real 15-component blueprint reusing `business_dossier`/`value_engine`/`growth_engine` verbatim — its own docstring says "near-zero new computation"). `production_blueprint.py::build_production_missions_board()` already is the real substrate for ABB's "Business Pipeline"/"Blueprint Status" ask.

10 of 12 named sections directly duplicate existing `business_dossier`/`production_blueprint` fields; 2 are genuinely new (`competitor_map()` citing `competition`/`competitive_moat`; `risk_assessment()` citing `value_engine`'s real `at_risk` flag, not currently surfaced in either blueprint). 4 of 8 named estimates cite already-real fields (`investment_score()`'s `engineering_cost`/`recurring_revenue_potential`/`automation_potential`, `value_engine`'s `board_summary.expected_roi`); 4 (expected monthly/yearly revenue, break-even time, market durability) have zero real signal anywhere, confirmed by repo-wide grep — honestly `Unknown`, never invented. `execution_phases()` maps `orchestrator.types.EXECUTION_ORDER`'s 5 real stages onto the directive's phase shape with real, deterministic objectives/tasks/dependencies/rollback-plan citations per stage (never a fabricated task list) — `required_ai_models` is deliberately cited once, company-wide (`ai_capability.orchestrator.resource_allocation_status()`), since mapping the 6 `AGENT_PROMPTS` UI personas onto pipeline stages would have been a real fabrication (those personas are manually-triggered, not wired into the automated pipeline).

`business_blueprint(niche)` calls `production_blueprint.build_production_blueprint()` + `value_engine.compute_value_profile()` + `capital_allocation_engine.investment_score()` exactly once each — `production_blueprint.py` gained one small additive field (`expansion_strategy`, already computed internally, previously not re-exposed) specifically to avoid a second, redundant `annotate_decision()` call; `competitor_map()`/`risk_assessment()` both gained an optional `profile=None` injection param for the same reason. Mission Control: `build-business-blueprint` (per-niche, `ACTION_REGISTRY`, 15-minute timeout) + a button in `mission_control.html`'s Opportunities tab; `business-pipeline`/`execution-phases` (both no-input, `SERVICE_REGISTRY` + `mission_control_executive_v1.html` panels, measured live at ~11s/~2s — no timeout override needed).

### Final Executive Directive — Autonomous Operations

`autonomous_operations_status.py` (new, ADR-142, 2026-07-29) answers the founder's "FINAL EXECUTIVE DIRECTIVE" (fully autonomous digital enterprise, Founder approval only for strategic/financial/legal decisions) with a real, citation-only status map of its ~21 named activities. Before any code was written, the literal ask was found to conflict with 4 standing, explicitly human-gated systems — `evolution_queue.py`'s Execute step ("must not be loosened without asking again"), `capital_allocation_engine.py`'s "the engine recommends, the Founder decides", business retirement (no real system exists, by design), `channels/publish_protection.py`'s Founder Protection layer for new/elevated-risk channels — flagged via `AskUserQuestion`; **the Founder's answer: keep all 4 gates exactly as-is**. A second finding: this exact "always-on autonomous company" question was already asked and declined 3x in prior sessions (`master_loop.py`, ADR-107 → ADR-110 → ADR-115) — this directive does not resurrect that daemon; it only extends the already-approved `factory_loop.js` tick (already runs every ~10 minutes once started).

`activity_status()` tags each activity `automatic` (10, already real — discover/validate/build/test/publish/monitor-sales/improve-intake/report/detect-risks, all pre-existing tick behavior), `automatic_new` (2 genuinely new, confirmed-safe additions below), `human_gated_by_design` (the 4 protected gates + business retirement + pricing, unchanged), or `ambiguous_not_touched` (3 deferred, disclosed, not guessed at: competitor auto-refresh risks a live network call from a read path; `sync_outcomes()`'s exact per-tick automaticity via the orchestrator registry dispatch couldn't be confirmed with full certainty; `growth_engine.evaluate_channel_expansion()` is real but not tick-wired). `autonomous_operations_summary()` aggregates real counts + the 4 protected-gate names + the `master_loop.py` precedent citation.

The 2 new `factory_loop.js` daily tick additions (same once-per-calendar-day gate as the existing 4 daily reports): `maybeGenerateDailyKnowledgeGraph()` calls `knowledge_graph.build_graph()` (confirmed zero prior tick callers via direct grep) and persists it via `knowledge_graph.build.save_snapshot()` (already existed, never called until now) to `data/knowledge_graph_snapshot.json` (gitignored — a disposable, deterministically-rebuildable cache, not a ledger); `maybeGenerateBusinessBlueprintsForNewAcceptedDecisions()` calls `autonomous_business_builder.generate_pending_business_blueprints()` (new — for every real ACCEPTED decision without an already-recorded blueprint, generates up to a capped batch of 2 per call, ~16s/niche real compute) and dedups against the real, committed append-only ledger `data/generated_business_blueprints.jsonl`. Both are read-only/no-execution — neither touches any of the 4 protected gates. Mission Control: `autonomous-operations-status` (`SERVICE_REGISTRY`, no-input, cheap) + a `mission_control_executive_v1.html` panel.

### Executive Brain

`executive_brain.py` (new, ADR-144, 2026-07-30) answers the founder's "Executive Brain V2" directive — consume every intelligence system, produce ONE executive decision per cycle, no conflicting actions. Before any code was written, research found the literal ask conflicted with the same 4 human-gates ADR-142 already reconfirmed (Phase P1's "no human intervention" reopens the always-on-daemon question declined 4x — `master_loop.py`/ADR-107→110→115→142) and overlapped almost entirely with already-real aggregators (`strategic_intelligence_core.build_executive_brief()`, `global_opportunity_exchange.py`, `capital_allocation_engine.py`, `evolution_queue.py` — plus this same session's own Architecture Stability Review, which had just committed "9+ overlapping Mission Control panels" as this factory's top maintainability risk). Both flagged via `AskUserQuestion`; **the founder's answers: keep execution human-gated, consolidate rather than add an 11th layer**.

`build_executive_directive()` calls those 4 systems + `channels/ledger.py::revenue_trend()` exactly once each — never a second competing computation — and adds only two genuinely new pieces: (1) `_candidate_directives()`/`_arbitrate()`, which tags every real candidate action already surfaced by those systems with the founder's own named Priority tier (1 System Stability → 5 Self Evolution) and picks the single lowest-tier one as the cycle's directive, honestly reporting a genuine same-tier tie as `SPLIT` rather than resolving it arbitrarily (same discipline as `galaxy_council.py`'s own disagreement handling); (2) a permanent append-only ledger (`data/executive_directives.jsonl`, same convention as `galaxy_council.py`'s `data/council_recommendations.jsonl`), mechanically parsed into a new `ExecutiveDirective` Knowledge Graph node type (`knowledge_graph/build.py`'s `_executive_directive_nodes()`, edges to a real `Proposal` node only when resolvable, never guessed). `requires_founder_approval` is always `True` — this module never calls `evolution_queue.approve_proposal()`, never reallocates capital, never publishes, never retires a business; every one of the 4 ADR-142 protected gates is untouched.

A real design bug was found and fixed during implementation: the first draft's Mission Control read handler defaulted to recording every view into the "permanent" ledger, which would have silently duplicated it on every page refresh — directly violating the directive's own "no duplicated knowledge" requirement. Fixed by splitting into `_executive_brain_directive()` (the live Mission Control view, `record_ledger=False`, verified live to never touch disk) and `_generate_daily_executive_directive()` (the one real path that grows the ledger, called only by `factory_loop.js`'s new daily tick step, same once-per-calendar-day gate as every other daily report). Verified live against real factory data: the expensive endpoint (chains 3 full-portfolio scans, measured ~59s, 120s Mission Control timeout) correctly arbitrated 43 real candidates down to a real Tier-1 finding (an unpinned npm dependency warning from `resilience_monitor`), not a fabricated one. Phase P2's "detect weak modules, propose, validate, run regression, reject unsafe, promote safe" was found to be, functionally, a re-description of the already-real `evolution_queue.py` pipeline (ADR-133/143) plus this session's own Architecture Review process — not rebuilt; the one genuinely missing piece (auto-running regression tests against a *proposed* change before founder review) is disclosed as an honest gap, not silently assumed to exist. Mission Control: `executive-brain` + `executive-directives-history` (`SERVICE_REGISTRY`), placed first in `mission_control_executive_v1.html`'s panel list per the founder's "single source of truth" ask, with the 4 most directly-reused existing panels cross-referenced in place rather than deleted (backward-compatible, per the founder's own "consolidate" answer).

### Executive Decision Memory

`executive_decision_memory.py` (new, ADR-145, 2026-07-30) answers the founder's "P0 Only" follow-up directive — record every executive decision with a unique ID, prevent duplicates, detect conflicts, explain WHY, track outcomes, improve from history. Research found almost the entire objective list already real: `decision_engine/` (ADR-050/076) already owns `decision_id`/`decided_at`/`status`/`reasoning`/`evaluation_snapshot`/`alternatives_rejected`/`expected_outcome` for niche decisions, `decision_engine/feedback.py::sync_outcomes()` already tracks outcomes over time, `decision_engine/learning.py::recalibration_report()` already computes real per-dimension historical-evidence statistics (deliberately never auto-applied — "needs a separate decision to apply it"), and `knowledge_graph/build.py` already has real `Decision`/`Outcome` nodes plus a generic `query_related()` traversal primitive. Two things were genuinely missing, confirmed by reading the code: `decision_engine/store.py::append_decision()` has zero duplicate-prevention logic, and no function anywhere assembled a single "why was this decided" narrative.

Additive-only fields on `executive_brain.py`'s own ledger (ADR-144's "Reuse Executive Brain," taken literally): `_make_directive_id()` (same sha256-truncate convention as `decision_engine.types.make_decision_id()`, applied to a domain object that isn't always niche-scoped), `_confidence_estimate()` (disclosed heuristic — `high`/`low`/`n/a`, never a fabricated number), `_detect_repeat()` (the real duplicate-prevention mechanism — an identical action+tier recommended again is honestly tagged `is_repeat_of` the prior decision_id, still recorded since the underlying alert may genuinely still be active, never silently dropped). `executive_decision_memory.py` adds the two genuinely new functions: `detect_niche_conflict()`/`detect_ledger_conflicts()` (real, mechanical, keyword-based — same discipline as `evolution_queue.py`'s `SENSITIVE_AREA_KEYWORDS` — never semantic/AI judgment) and `explain_decision(decision_id)` (dual-lookup across both real ledgers, reuses `knowledge_graph.build.query_related()` directly, prefers the last saved snapshot over a live rebuild). `list_decision_memory()` is the Mission Control unified read-model — a pure merge-sort of both real ledgers, no new computation. Zero new execute-capable code path; duplicate detection is a read-side annotation, never a write-side rejection. Mission Control: `decision-memory`/`decision-memory-conflicts`/`decision-memory-explain` (the last takes `?decision_id=`, same query-param precedent as the existing `product-concept-comparison` panel), distinct from the pre-existing niche-only `decision-history` panel.

### Other Python utilities

- `market_analyzer.py` — Static niche scoring logic; called live via `POST /api/market-analyze` (`server.js`).
- `quality_doctor.py` and its `POST /api/qa-check` route were removed (Executive Intelligence Core, Round 7, 2026-07-29): the module was a confirmed-fake legacy prototype (found in the Strategic Phase audit, 2026-07-19) — every `_check_*()` method appended a fabricated "fix" string without regenerating/redrawing/repricing anything real, and `health_score`/`ready_to_publish` weren't derived from any real check. It had zero real callers (no UI button, no pipeline stage) even after being disclosed via a `warning` field, and the new directive's own explicit "no fake metrics" rule was the clean trigger to finally remove it rather than leave a known-fake module live. `ai_doctor.py` was already its real, non-fabricated replacement (EOS Phase 2, 2026-07-19). `inspectors.py`'s Dual Inspection remains the real, load-bearing QA gate every actual generated product is checked against.
- `cover_designer_v2.py` — The live cover-generation engine, imported by `book_generator.py`, `inspectors.py`, `profit_oracle.py`, and `server.js`. (An older `cover_generator.py` existed as an unwired Day-05 scratch script and was removed 2026-07-13.)
- `niche_validator_v2.py` — The live niche-validation engine (imported as `NICHE_VALIDATOR` by `book_generator.py`/`profit_oracle.py`). (An older `niche_validator.py` v1 existed, claimed a `/analyze-niche` endpoint that no longer exists in `server.js`, and was removed 2026-07-13.)
- `audit_seed.py`, `hive_logbook_generator.py`, `seed_english_book.py` — standalone CLI tools run manually, by design not imported by any other module ("Zero factory impact" per `audit_seed.py`'s own docstring; `seed_english_book.py`'s own docstring: "Standalone... Does NOT touch the live factory pipeline. Safe to run/delete."). `hive_logbook_generator.py` produced the real HiveNotes KDP product (`seeds/hivenotes/`) entirely outside the generic `book_generator.py` pipeline and outside `factory_loop.js`'s awareness. `seed_english_book.py` produced the real `seeds/morning-focus-journal-for-remote-workers-v1.docx` — it imports `book_generator._parse_sectioned_book` (read-only reuse) but nothing in the live pipeline calls it back; until this line was added (STRUCTURAL_DIAGNOSIS.md disease #11) its standalone status wasn't documented outside its own docstring and a bare filename in `ARMS_ARCHITECTURE_INPUT.md`'s tree snapshot.

### No scheduler exists

Nothing in this repo runs anything on a timer: `factory_loop.js` (the Golden Hunter loop) has no cron, no `package.json` script, and no CI workflow — it only runs when invoked manually (`node factory_loop.js`). It also does not auto-process Human-in-the-Loop approvals: it only counts files in `pending_review/queue/` and writes an alert to `NEEDS_REVIEW.md`; a human must run `scripts/process_approved_drafts.py` manually after approving a draft. Confirmed by full-repo audit, 2026-07-13.

### `index.html` encoding

The file is saved as **UTF-16 LE with BOM**. When reading or editing it, ensure your editor preserves this encoding, or the Arabic text and emoji will corrupt.
