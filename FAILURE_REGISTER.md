# Galaxy Forge — Failure Register

**Date:** 2026-08-07 | Every real problem found during the Phase 13 reality test, classified P0–P4. No item here is theoretical — each was reproduced live or confirmed by direct code inspection this round.

**Classification:** P0 = company cannot safely operate. P1 = major commercial/financial risk. P2 = important operational weakness. P3 = improvement opportunity. P4 = cosmetic/low priority.

---

| ID | Finding | Class | Evidence | Recommended action |
|---|---|---|---|---|
| F1 | No Paddle sandbox is configured anywhere — `PADDLE_API_BASE` is hardcoded to production. Payment/refund/checkout testing is structurally impossible without real money. | **P1** | `channels/paddle_publisher.py:44` | Configure a real Paddle sandbox account + `PADDLE_SANDBOX_API_KEY` if pre-launch payment testing is ever required; otherwise explicitly accept this as a permanent constraint |
| F2 | The one real shipped product (EU AI Act Toolkit) has no real checkout URL stored anywhere in this factory's own records. | **P1** | `product_master_catalog.py` live output, `checkout_url: null`; confirmed no `publish_attempt` ledger event exists for this product at all | Generate a real checkout URL via the already-proven `create_checkout_transaction` path (ADR-183), with founder authorization, and record it |
| F3 | The real live Paddle product creation for the EU AI Act Toolkit was never recorded in `data/sales_ledger.jsonl` — it happened via a direct script call outside the standard `record_publish_attempt()` path. | **P1** | Confirmed: 0 `publish_attempt` events exist for this product in the real ledger | Backfill a real, disclosed retroactive ledger entry, or accept the gap and ensure all future real publishes go through the standard path |
| F4 | `decisions.jsonl`'s most recent formal decision for the EU AI Act Compliance Toolkit niche is `DEFERRED`, while a real product was shipped and a real Paddle listing created for it via a separate manual-evidence path. The formal decision record does not reflect the niche's true commercial status. | **P2** | Direct read of `data/decisions.jsonl`, 20 records for this niche, latest `decided_at: 2026-08-06`, `status: DEFERRED` | Record a corrective decision entry once real evidence is fully consolidated, or document the divergence permanently in the niche's own record |
| F5 | `product_master_catalog.py`'s record for the one real shipped product reports `description`/`source_files`/`version`/`target_customer`/`target_market` as `"Unknown"`, even though real values exist in `books/_generation_log.jsonl`. | **P2** | Live catalog output vs. real generation-log entry for the same title | Extend `product_master_catalog.py` to cross-reference `books/_generation_log.jsonl` for these fields (a real, scoped follow-up) |
| F6 | Paddle's HTTP retry logic (`_request_with_retry`) never retries a 429 (rate-limit) response — only 5xx server errors trigger a retry. This differs from `book_generator.py::groq_chat()`'s real `Retry-After`-aware retry. | **P2** | Direct code read, `channels/paddle_publisher.py:55-70` | Add real `Retry-After` handling to Paddle's retry helper, mirroring the Groq fix already shipped elsewhere in this factory |
| F7 | No webhook receiver exists anywhere in this factory — all payment/refund state is discovered by polling (`check_payment_status()`), never pushed. A real-time Paddle event (e.g. a customer-initiated refund) would not be reflected until the next poll. | **P2** | Confirmed by direct grep: 0 matches for any webhook-signature-validation code anywhere in the repo | Real-time detection is not required at current scale (0 real transactions); revisit if/when real transaction volume exists |
| F8 | `channels/paddle_publisher.py::list_products()`'s raw JSON-parsing does not itself catch a malformed-response `ValueError` — it propagates uncaught at that layer. (The higher `PaddleArm.list_products()` layer DOES catch it correctly — this is a real but low-severity gap, not a caller-facing defect.) | **P3** | Live failure-injection test this round: raw `paddle_publisher.list_products()` call raises an unwrapped `ValueError`; the same call through `PaddleArm.list_products()` correctly returns `{"status": "ERROR", ...}` | Add an explicit `except ValueError` at the publisher layer for defense-in-depth and clearer error messages, low priority given the real caller boundary already handles it |
| F9 | No unified `correlation_id` concept exists — domain-specific IDs (`decision_id`, `product_source_id`, `request_id`) link related events within their own domain but there is no single ID that traces one real customer transaction across decision → product → publish → sale → reconciliation. | **P3** | Direct grep: 0 matches for `correlation_id`/`trace_id` anywhere in the repo | Not urgent at 0 real transaction volume; worth introducing before real transaction volume grows enough to make manual tracing painful |
| F10 | The customer-facing trust pages (`privacy-policy.html`/`terms-of-service.html`/`refund-policy.html`) still carry real, unfilled legal placeholders (business name, jurisdiction). Already tracked as Finding T1 in `COMMERCIAL_READINESS_REPORT.md` Part B — re-confirmed still open this round. | **P1** | `COMMERCIAL_READINESS_REPORT.md` Part B | Requires the founder's real legal-entity/jurisdiction decision — not an engineering task, cannot be closed by code |

## What was NOT found (explicitly, so this isn't read as an exhaustive-looking-but-incomplete list)

- **No P0 finding.** Nothing discovered this round would make continued safe operation of the existing (dry-run-heavy, honestly-$0-revenue) system unsafe.
- No hardcoded secret, no logged API key, no fabrication path, no duplicate decision record, no orphaned Paddle product were found — each explicitly tested, see `SECURITY_TEST_REPORT.md`/`DATA_RECONCILIATION_REPORT.md`.

---

*See also: `END_TO_END_TEST_REPORT.md`, `PLATFORM_RELIABILITY_REPORT.md`, `AUTOMATION_GAP_REPORT.md`.*
