# OVERNIGHT_INVESTIGATION_20260712.md — تحقيق في التشغيل الليلي (2026-07-11 → 2026-07-12)

**التاريخ:** 2026-07-12
**السياق:** ~12 ساعة مرّت منذ ختام جلسة 2026-07-11. `factory_loop.js` كان يُفترَض أن يعمل باستمرار (شُغِّل عبر `start_factory.bat` في ~20:05 بالتوقيت المحلي مساء 07-11).
**طبيعة الوثيقة:** تحقيق قراءة فقط. لا تنفيذ، لا إصلاح. كل رقم هنا من فحص مباشر لملفات حقيقية، لا تقدير.

---

## 1. الأرقام الخام

### `data/golden_hunter_events.jsonl`
- **16 سطراً إجمالاً.**
- **كل الأسطر الـ16: `action: "attempted"`** — صفر `"skipped"` (لا فشل قراءة/تسعير خلال هذه الفترة).
- **نيتش واحد فقط** ظهر في كل الـ16 محاولة: "مخطط شهري قابل للطباعة العودة للمدارس" — نفس الملاحظة من `ADR-011` تتأكد: `market_hunter.py` لم ينتج مرشّحاً أعلى مختلفاً طوال هذه الفترة.
- **مصادر التسعير:** `butter_price` في 13 محاولة (صحيح، بعد إصلاح ADR-010)، `None` في 3 محاولات (قديمة، قبل الإصلاح — من 14:13 يوم 07-11، خارج نطاق هذا التحقيق).
- **4 محاولات فقط حدثت فعلياً أثناء الليل** (بعد 19:00 UTC يوم 07-11): الساعات 20:55، 00:55، 06:37، 06:47 (كلها UTC) — بدل ما يقارب 12 دورة متوقَّعة لو استمر الفاصل الطبيعي (10 دقائق) بلا انقطاع طوال 12 ساعة.

### `factory_loop.log`
- **192 دورة (tick) مسجَّلة إجمالاً.**
- **صفر خطأ:** لا سجل واحد بـ`action: "error"`، ولا تشخيص واحد بحالة `"loop_error"` عبر كل الـ192 دورة.

### `NEEDS_ATTENTION.md`
- **لم يظهر إطلاقاً** طوال الليل — تحقَّقتُ في بداية هذا التحقيق ونهايته، غائب في الحالتين.

---

## 2. الفجوات — العدد والتوقيت الدقيق

**فجوتان أكبر من الفاصل الطبيعي (10 دقائق)،** بعد نمط منتظم استمر من 17:56 حتى 18:55 (07-11):

| # | البداية (UTC) | النهاية (UTC) | المدة |
|---|---|---|---|
| 1 | 2026-07-11T18:55:23Z | 2026-07-11T20:55:25Z | **~2 ساعة و2 ثانية** |
| 2 (الأطول) | 2026-07-11T20:55:25Z | 2026-07-12T06:37:15Z | **~9 ساعات و42 دقيقة** |

بعد الفجوة الثانية مباشرة: دورتان متقاربتان جداً — `06:37:15.793Z` ثم `06:37:19.200Z` (**4 ثوانٍ فقط بينهما**، لا 10 دقائق) — ثم عاد الإيقاع الطبيعي فوراً (`06:47:12Z`, `06:57:12Z`, ...).

---

## 3. السبب — الاستنتاج من الأدلة المباشرة

**لا دليل انهيار برمجي واحد.** الاستنتاج الأرجح: **الجهاز دخل وضع سكون (Sleep) في نظام التشغيل**، لا عطل في `factory_loop.js` نفسه. الأدلة:

1. **صفر أخطاء مسجَّلة** عبر 192 دورة — انهيار حقيقي (استثناء غير مُمسَك) كان سيترك أثراً في `factory_loop.log` عبر `process.on('uncaughtException'/'unhandledRejection')` (كلاهما مُفعَّل في `factory_loop.js`). غيابهما التام دليل سلبي قوي ضد فرضية الانهيار.
2. **نفس معرّفات العمليات (PIDs)** التي بدأت مساء 07-11 (~20:05 محلياً) **لا تزال تعمل الآن** بعد التحقيق — لم يُعَد تشغيل أي عملية جديدة. انهيار حقيقي كان سيتطلّب إعادة تشغيل يدوية (لا آلية إعادة تشغيل تلقائي موجودة — `AUTOMATION_GAPS_REPORT.md` §2، "لا مُشرِف عملية").
3. **أول دورة بعد الفجوة الكبرى فشلت بمهلة زمنية:** `golden_hunter | failed | انتهت مهلة market_hunter.py (60 ثانية)` — توقيع نموذجي لجهاز يستيقظ للتو (قرص/شبكة بطيئان مؤقتاً)، لا خطأ منطقي في الكود.
4. **النبضتان المتقاربتان (4 ثوانٍ)** مباشرة بعد الاستيقاظ توقيع معروف لمؤقّت `setInterval` في Node.js يُطلَق فوراً عند استئناف عملية كانت متوقّفة تماماً (لا تُعالج الوقت الضائع تدريجياً، بل "تستيقظ" وتُطلق الدورة المتأخرة بأسرع ما يمكن).

**ملاحظة جانبية غير محسومة:** عملية `node.exe` سادسة جديدة (PID مختلف) ظهرت اليوم بتوقيت بدء `12/07/2026 09:01:57` محلياً — بعد استئناف النبض بقليل. **لم يُحقَّق في مصدرها** (قد تكون غير متعلقة إطلاقاً — عملية فرعية لـ`n8n` مثلاً). لا دليل أنها نسخة ثانية من `factory_loop.js` (لا تكرار مطابق في الأحداث المسجَّلة يدعم فرضية "نسختان متزامنتان" بشكل قاطع)، لكنها تستحق نظرة إن تكرر النمط.

---

## 4. نتيجة `python scripts/readiness_certificate.py` (وقت هذا التحقيق، ~07:28 UTC)

**القرار: 🔴 BLOCKED**

| المعيار | الحالة | الدليل |
|---|---|---|
| `continuous_operation_48h` | ⏳ PENDING | السلسلة الحالية بدأت 06:37:15Z فقط (0.8 ساعة) — **الفجوة صفّرت العدّاد بالكامل**، رغم أن العملية الفعلية عمرها أطول بكثير |
| `distinct_niches_ge_3` | ⏳ PENDING | لا يزال نيتش واحد فقط من 3 مطلوبة |
| `needs_attention_free_24h` | ✅ PASS | لا مشاكل — يتماشى مع §1 أعلاه |
| `constitutional_pricing` | ❌ FAIL | 3 من 18 محاولة (تراكمياً) لا تزال تحمل السعر القديم الخاطئ ضمن نافذة 24 ساعة |
| `fresh_discovery_regular` | ✅ PASS | `golden_opportunities.json` حديث، `market_hunter` يعمل بانتظام |

---

## 5. ما تعلّمناه من هذه الليلة

1. **`factory_loop.js` نفسه موثوق** — صفر أخطاء، لا تسريب حالة، استأنف العمل بشكل صحيح بعد التوقف الطويل من نفسه دون أي تدخل بشري.
2. **معيار "48 ساعة تشغيل مستمر" في `readiness_certificate.py` (ADR-011) لن يتحقّق أبداً على هذا الجهاز** ما دام وضع السكون مفعَّلاً — أي نوم للجهاز (حتى لثوانٍ حسب حساسية `MAX_TICK_GAP_MINUTES=20` في السكريبت) يُصفِّر العدّاد من جديد. هذه ليست مشكلة كود — هي واقع تشغيل على لابتوب شخصي لا خادماً دائم التشغيل.
3. **`NEEDS_ATTENTION.md` لم يُصمَّم لرصد هذا النوع من الانقطاع** — شروط التفعيل الثلاثة (فشل صريح، سلسلة توقّف Golden Hunter، سلسلة تسعير احتياطي) لا تغطي "الجهاز نام لساعات" لأن لا خطأ حدث فعلياً ليُرصَد. فجوة حقيقية غير موثَّقة سابقاً.

## توصية واحدة

**امنع الجهاز من الدخول في وضع السكون (Windows Power Settings → Sleep → Never، أو ما يعادلها) قبل إعادة التشغيل** — بدون هذا، أي محاولة مستقبلية لإثبات "48 ساعة تشغيل مستمر" ستفشل لنفس السبب بصرف النظر عن أي إصلاح كود.
