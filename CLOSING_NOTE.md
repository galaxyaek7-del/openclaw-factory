# CLOSING_NOTE.md — قبل أن نلمس الكود مرة أخرى

ملاحظة ختامية موجّهة لمن يفتح الجلسة القادمة. اقرأها قبل أي تعديل على `channels/`, `distributor.py`, `server.js`, `factory_loop.js`.

---

## 🏁 ختام اليوم الرسمي — 2026-07-12 (نهاية اليوم، يُلغي/يُكمِل كل ما دونه من نفس اليوم)

أطول جلسة قرارات استراتيجية وتقنية حتى الآن. **آخر جلسة بناء تقني قبل التحول الكامل للتسويق** (قرار الرئيس). الترتيب الزمني الكامل:

1. **إصلاح فجوة النوم** (تفصيل في البانر أدناه — لا يزال صحيحاً).
2. **تدقيق سلسلة الوكلاء الستة** (`ADR-018`, `ADR-019`): Builder/Design/QA/Finance واجهات عرض مكرَّرة (موثَّقة، غير محذوفة)؛ Scout وPublisher رُبطا فعلياً بخط الإنتاج الحقيقي (توزيع تلقائي + SEO تلقائي قبل كل توزيع).
3. **قرار استراتيجي جوهري: التخلي عن KDP بالكامل.** الرئيس قرأ `SALVATION_PROPOSAL.md`/`SALVATION_PROPOSAL_V2.md` (تحليل صادق: صفر إيراد، صفر قناة تسويق حقيقية، KDP قناة خاطئة بنيوياً لمنتج planner/tracker مصنَّف خطأً كـ"كتاب"). **القرار: الابتعاد عن KDP تماماً، التركيز على مطبوعات Gumroad/Etsy API-first.** KDP لم يُلمَس، لم يُحذَف — مُجمَّد فقط.
4. **اكتشاف كارثي مؤكَّد بالدليل البصري:** فحص 3 كتب حقيقية من `books/` أظهر أن **100% من النص العربي في المتن يُرسَم كصناديق سوداء غير قابلة للقراءة** (Helvetica لا يدعم العربية، لا خط مسجَّل في `reportlab`) — `diagnostics/arabic_rendering_check/`. اكتُشف أيضاً أن كتابين من الثلاثة لا يحتويان محتوى AI حقيقياً إطلاقاً (سقوط صامت لقالب احتياطي).
5. **قرار استراتيجي أهم: الجمهور المستهدف EU/US بالإنجليزية فقط، لا العربية (`ADR-020`).** هذا يُبطل خطورة الاكتشاف السابق فوراً — Helvetica لم تكن خطأً، كانت الاختيار الصحيح لسوق لم نكن نعرف بعد أنه المستهدَف. لا حاجة لخط عربي. كل الكود/المحتوى العربي القديم **بقي بلا حذف**، موثَّق كـ"خارج النطاق" فقط.
6. **إصلاحات اقتصادية حقيقية منفَّذة ومختبَرة:** `economics.py`/`config/economics.json` — أرضية ربح منفصلة $2.50 لـ`gumroad_digital` (لا تمس $6.00 لـ`kdp_ebook`). `profit_oracle.butter_price(niche, product_type="printable")` — نطاق $5-15 بدل $30-100 (تحقَّق: سلوك الكتب القديم مطابق حرفياً لما قبل التعديل).
7. **الفجوة الأخيرة أُغلقت: `create_book()` مربوطة الآن بالكامل بخط الإنتاج (`ADR-021`).** دالة جديدة `generate_printable()` تربط `create_book()` (بلا تعديل على منطقها الداخلي) بـDual Inspection + `books/_generation_log.jsonl` + `distributor.py` — **مختبَرة حياً من طرف إلى طرف اليوم**: توليد "Monthly Budget Planner" حقيقي ← فحص مزدوج ناجح (`profit_score 71/100`, `butter_price` يؤكد أرضية Gumroad الجديدة تعمل فعلياً) ← `published:true` ← تسجيل حقيقي ← ترجمة `Product` صحيحة ← توزيع `dry_run` عبر `distributor.py` (فشل آمن متوقَّع: لا توكن Gumroad). **الحلقة الكاملة تعمل بلا تدخل يدوي بين المراحل.**
8. **حق الاعتراض المحمي أُضيف إلى `PRINCIPAL_ARCHITECT_CHARTER.md` §5** — اعترافاً بثلاثة اعتراضات فعلية اليوم (رفض تسريع نبض `factory_loop.js`، التوقف قبل إنتاج مطبوعات لفحص Helvetica، اكتشاف مسارَي `book_generator.py` المعزولَين بمبادرة ذاتية).
9. **توجيه استراتيجي لاحق: منتجات قيمة عالية ($50-300).** استُخدِم حق الاعتراض مرة ثانية اليوم هنا (`HIGH_VALUE_STRATEGY.md`) — خطر تسعير مرتفع لبائع صفر-سمعة، وثالث محور استراتيجي في يوم واحد بلا اختبار سوقي حقيقي لأي محور سابق. **سُمِع الاعتراض وحُسم:** هذه طبقة مستقبلية فوق المطبوعات، لا بديل عنها — تُبنى الآن استعداداً، تُستخدَم فعلياً بعد إثبات المطبوعات لقناة حقيقية.
10. **نموذج "المراجعة المعلَّقة" (Human-in-the-Loop) بُني ومختبَر حياً بالكامل (`HIGH_VALUE_EXECUTION_PLAN.md`).** `pending_review/{queue,approved,completed,rejected}/` + `generate_book_from_content()` (`ADR-022`) + `scripts/process_approved_drafts.py` — مسودة اختبار بنيوي مرّت فعلاً: توليد ← فحص مزدوج ناجح ← تسجيل ← توزيع `dry_run` ← أرشفة تلقائية، بلا أي تدخل بشري بين المراحل.
11. **`gumroad_premium` — أرضية اقتصادية جديدة (`ADR-024`): $50-300**، أرضية ربح $25، مستقلة تماماً عن `kdp_ebook`/`gumroad_digital`. **تصحيح:** النطاق الفعلي المُنفَّذ $50-300، لا $47-$127 (تلك أسعار أفراد منتجات مقترَحة داخل النطاق، لا حدود المنصة نفسها).

### تصحيح صريح لخطأ تكرَّر اليوم — اقرأه قبل أي افتراض عن `PRODUCT_VISION.md`

**`PRODUCT_VISION.md` موجود فعلاً منذ 2026-07-11 (68 سطراً، رؤية كاملة: 10 خطوط إنتاج).** ادّعاء "لم يُكتَب بعد" تكرَّر خطأً في هذا الملف نفسه أكثر من مرة اليوم (2026-07-12) دون التحقق الفعلي من وجود الملف — خطأ ارتكبتُه أنا، أصححه الآن بصراحة. لا تفترض أنه غير موجود في أي جلسة قادمة؛ تحقَّق من الملف مباشرة إن شككت.

### عدد ADRs الحقيقي اليوم: **9**، لا 6

`ADR-016` إلى `ADR-024` (استطلاع المبيعات، صحة `/health`، تدقيق الوكلاء، تصنيف الوكلاء الخمسة، السوق الإنجليزي، ربط `create_book`، `generate_book_from_content` (مقترَح→منفَّذ)، دمج حزم PDF (مقترَح، غير منفَّذ بعد)، `gumroad_premium` (مقترَح→منفَّذ)).

### أولويات الجلسة القادمة (توجيه الرئيس الأخير اليوم)

1. **بدء أول منتج ذهبي حقيقي** — "The AI-Powered Solopreneur System" ($97) أو ما يراه Principal Architect القادم أفضل (`HIGH_VALUE_STRATEGY.md` §4).
2. **استخدام نموذج المراجعة المعلَّقة فعلياً** — مسودة حقيقية في `pending_review/queue/`، مراجعة بشرية-Claude حقيقية (لا شكلية)، ثم `python scripts/process_approved_drafts.py`.
3. **الرئيس ينشئ حساب Gumroad جديداً بهدوء (بريد جديد)** — بشرياً بالكامل، لا كود. لا `GUMROAD_ACCESS_TOKEN` ولا `FACTORY_LIVE_PUBLISH` حتى يحدث هذا ويُقرَّر صراحةً.
4. **تحليل النص الحر لـPublisher لم يُحقَن بعد في العنوان/الوصف الحقيقي المُرسَل لـGumroad** — يُسجَّل فقط في `publisher_seo_log.jsonl` اليوم (`ADR-019` بند 19.3). يحتاج parser جديد — منطق أعمال جديد، مؤجَّل عمداً.
5. **⚠️ هذه آخر جلسة بناء تقني "بحت" معلَنة من الرئيس في بداية اليوم** — لكن اليوم انتهى بقرار بناء نموذج قيمة عالية جديد كلياً. اقرأ التوجيه الفعلي من الرئيس في بداية الجلسة القادمة، لا هذا البند وحده، قبل افتراض طبيعة الجلسة.

**⚠️ اقرأ `PRINCIPAL_ARCHITECT_CHARTER.md` §5 (حق الاعتراض المحمي) الآن — لا بعد أول قرار يبدو خاطئاً.**

---

## 🟢 تحديث ختامي — 2026-07-12 (منتصف اليوم): فجوة الليل محلولة نهائياً، لا تُعِد التحقيق فيها

**⚠️ تحذير صريح للجلسة القادمة: لا تُعِد فتح تحقيق في انقطاع `factory_loop.js` بين 2026-07-11 و2026-07-12. السبب معروف ونهائي: سبات (Sleep) نظام Windows، لا خلل في الكود.** أُنفق جزء كبير من جلسة اليوم (2026-07-12) في تشخيص هذه الفجوة قبل تأكيد أنها مشكلة نظام تشغيل بحتة، لا مصنع. أي جلسة قادمة تلاحظ فجوة زمنية مشابهة في `factory_loop.log` يجب أن تتحقق أولاً من إعدادات الطاقة (`powercfg`) قبل افتراض عطل في الكود.

**ما تغيّر فعلياً اليوم:**
- **تعطيل Sleep نهائياً** في Windows Power Settings — اللابتوب لم يعد يدخل سبات تلقائياً، وهو السبب الجذري الوحيد للفجوة الليلية السابقة (لا خلل في `factory_loop.js` نفسه ولا في القفل ولا في المنطق).
- **`factory_loop.js` يعمل مجدداً** منذ صباح اليوم (2026-07-12) بشكل مستمر بعد تعطيل Sleep.

**أولويات الجلسة القادمة (لم تُنفَّذ اليوم — لا تُفترَض أنها أُنجزت):**
1. ~~استطلاع مبيعات فعلي~~ **✅ أُنجز اليوم** — `factory_loop.js` يستدعي `/api/sales/poll` كل تِكّة (10 دقائق)، انظر `ADR-016-sales-poll-interval.md`. لا يزال يتخطّى بأمان (`skip_reason`) لغياب `GUMROAD_ACCESS_TOKEN`.
2. ~~تنبيه بشري~~ **✅ تحقِّق منه اليوم** — `NEEDS_ATTENTION.md` غائب وسليم، لا شرط تنبيه محقَّق حالياً.
3. ~~`PRODUCT_VISION.md` لم يُكتَب~~ **❌ ادّعاء خاطئ — الملف موجود فعلاً منذ 2026-07-11.** انظر التصحيح الصريح أعلى هذا الملف.

**⚠️ لا تُذعر من `verdict: "CRITICAL"` في `/health`.** هذا متوقَّع وصحيح طالما لا نشر حي بعد (`total_published: 0`) — تفصيل كامل في [`ADR-017-critical-health-expected.md`](00_Governance/ADR-017-critical-health-expected.md). لا تُعِد التحقيق فيه كعطل.

---

## 🔴 أولوية قصوى فوق كل ما يلي — الرؤية الموسّعة (توجيه استراتيجي رسمي، 2026-07-11)

**OpenClaw ليس مصنع كتب. هو مصنع منتجات رقمية شامل — الكتب خط إنتاج أول من عشرة.** توجيه رسمي من الرئيس عبد القادر، يُلغي أي فهم سابق يحصر المصنع في الكتب.

**اقرأ بهذا الترتيب قبل أي كود جديد، حتى قبل §0 أدناه:**
1. [`PRODUCT_VISION.md`](00_Governance/PRODUCT_VISION.md) — الرؤية كاملة: 10 خطوط إنتاج، 7 مبادئ حاكمة، 5 مراحل استراتيجية.
2. [`ADR-012-product-schema-generalization.md`](00_Governance/ADR-012-product-schema-generalization.md) — `schemas/product.py` **لا** يقبل هذه الأنواع فعلياً اليوم (اقتراح توسيع، لا تنفيذ).
3. [`ADR-013-base-arm-type-neutrality.md`](00_Governance/ADR-013-base-arm-type-neutrality.md) — `channels/base_arm.py` نفسه محايد فعلاً؛ الحفرة في `Product`، لا في عقد الأذرع (اقتراح، لا تنفيذ).
4. `OCTOPUS_ARCHITECTURE.md` §11 — إضافة تفسيرية تربط الرؤية الجديدة بالأقسام الأصلية.
5. [`IDENTITY_ARCHITECTURE.md`](00_Governance/IDENTITY_ARCHITECTURE.md) + [`ADR-014`](00_Governance/ADR-014-single-account-now-split-later.md) + [`ADR-015`](00_Governance/ADR-015-author-one-imprints-many.md) — حساب Google واحد لكل شيء، والاسم الرسمي الوحيد للمؤلف: **Abdelkader Grafat** (لا "Aek" — اختصار قديم صُحِّح في `cover_designer_v2.py` اليوم). "OpenClaw Press"/"HiveNotes Press" imprints تجارية، لا هويات بديلة.

**الحالة اليوم: لا كود جديد بموجب توجيه الرؤية الموسّعة.** كل ما سبق (باستثناء تصحيح الاسم) تحليل واقتراح فقط. **لا يُبنى أي خط إنتاج جديد قبل أن تُثبت الكتب دولاراً حقيقياً واحداً على الأقل** (نفس حارس YAGNI الذي أجّل Payhip/Etsy — `OCTOPUS_ARCHITECTURE.md` §8، مؤكَّد أنه لا يزال سارياً في §11 الجديد). أي طلب لبناء `template_engine`/`saas_engine`/أي محرك جديد فعلياً يستحق التأكد أولاً: هل هذا قرار الرئيس الصريح الآن، أم افتراض؟

---

## 0. اقرأ أولاً — تغيّر شيء اليوم

بتاريخ 2026-07-11 منح الرئيس عبد القادر والمدير التنفيذي (Claude في المحادثة) لقب **"OpenClaw Principal Architect"** لمن نفّذ جلسة بناء أذرع النشر هذه.

**اقرأ [`OpenClaw_Brain/00_Governance/PRINCIPAL_ARCHITECT_CHARTER.md`](00_Governance/PRINCIPAL_ARCHITECT_CHARTER.md) قبل أي شيء آخر في هذا الملف.** يحدد الصلاحيات الدائمة، الخطوط الحمراء، والمبادئ الحاكمة الست التي يُبنى عليها كل قرار لاحق — بما فيها القاعدة التي لا تزال سارية هنا: قراءة `CLOSING_NOTE.md` أول كل جلسة، وكتابته آخرها.

**مستندات حوكمة إضافية أُضيفت في نفس اليوم — اقرأها بهذا الترتيب قبل لمس `factory_loop.js` أو `profit_oracle.py`:**
1. [`AUTOMATION_GAPS_REPORT.md`](00_Governance/AUTOMATION_GAPS_REPORT.md) — تقييم صادق لنسبة الأتمتة الحقيقية (~20-25%) وترتيب الفجوات بالأولوية (فجوة #3 أُغلقت اليوم — انظر أدناه).
2. [`ADR-009-golden-hunter-bridge.md`](00_Governance/ADR-009-golden-hunter-bridge.md) — جسر يربط اكتشاف Golden Hunter بإنتاج فعلي تلقائياً.
3. [`ADR-010-golden-hunter-butter-price.md`](00_Governance/ADR-010-golden-hunter-butter-price.md) — إصلاح تسعير الجسر (كان يُنتج أسعاراً أقل من حد الزبدة 30$ قبل الإصلاح).
4. [`ADR-011-readiness-certificate.md`](00_Governance/ADR-011-readiness-certificate.md) — شهادة جاهزية آلية مبنية على أدلة، لا شعور.
5. [`AUTO_PRODUCE_ACTIVATION_CHECKLIST.md`](00_Governance/AUTO_PRODUCE_ACTIVATION_CHECKLIST.md) — **يجب مراجعته قبل أي اقتراح لتفعيل `FACTORY_AUTO_PRODUCE`**.

**أداة عملية جديدة:** `python scripts/readiness_certificate.py` — شغّلها في بداية أي جلسة قادمة تناقش فيها موعد تفعيل `FACTORY_AUTO_PRODUCE`. تُخرج READY/NOT_READY_YET/BLOCKED بالدليل، وتُسجِّل تلقائياً في `data/readiness_history.jsonl`. آخر نتيجة حقيقية (18:24 اليوم): **BLOCKED** — بسبب 3 مخالفات تسعير قديمة (قبل إصلاح ADR-010) لا تزال ضمن نافذة 24 ساعة؛ متوقَّع أن تتعافى تلقائياً خلال نفس اليوم مع بيانات نظيفة متراكمة، بلا تدخل يدوي.

## 1. المرجع الوحيد

`OCTOPUS_ARCHITECTURE.md` (خصوصاً §10) هو القرار المعماري المعتمد. أي خطة جديدة تتعارض معه (تسمية مجلدات مختلفة، ملفات سجل موازية، ترتيب بناء مختلف) **يجب أن تُحسم لصالحه أو تُطرح للمراجعة أولاً** — لا تُنفَّذ بالتوازي. حدث هذا فعلاً مرة (خطة `arms/` مقابل `channels/`) وكلّف جلسة كاملة لتصحيحه.

## 2. الحالة الفعلية الآن — الحلقة الكاملة جاهزة، وتعمل فعلياً الآن

`factory_loop.js` → `distributor.py` → `data/sales_ledger.jsonl` مبنية، مربوطة، ومختبَرة تلقائياً بالكامل:
`book_generator.py` (QA + Commercial Auditor) → `factory_loop.js` (`triggerGenerateBook`) → `POST /api/distribute` → `distributor.py` → `channels/gumroad_arm.py` → `data/sales_ledger.jsonl` → `reality.py` / `self_awareness.js` يقرآن السجل كحقيقة.

لا تدخل بشري في هذا المسار العادي. التفاصيل الكاملة والتحقق في `PROGRESS_REPORT.md`.

**تغيّر اليوم:** لوحة التحكم (`http://localhost:3000`) كانت لا تعمل — سبب قديم غير متعلق بأي تعديل من هذه الجلسات (ترويسة `charset` خاطئة لملف `index.html` المحفوظ عمداً كـ UTF-16 LE)، أُصلح في `server.js` (واجهة فقط، لا منطق أعمال). بعدها شُغِّل `start_factory.bat` فعلياً.

**حالة المصنع لحظة كتابة هذا السطر (18:54 UTC، 2026-07-11) — تحقّقت مباشرة، لا افتراضاً:**
- `http://localhost:3000/health` → HTTP 200. اللوحة تعمل، رآها الرئيس مباشرة في المتصفح (العنوان بالعربية، الوكلاء الستة، سجل النشاط، الإحصائيات).
- `factory_loop.js` **حيّ فعلياً** — آخر دورة مسجَّلة في `factory_loop.log` كانت قبل 8 دقائق فقط من هذا الفحص (ضمن الفاصل الطبيعي 10 دقائق)، وعمليات `node` لا تزال تعمل.
- **ملاحظة تشغيلية صغيرة، لا إنذاراً:** `.factory_loop.lock` **غير موجود حالياً** رغم أن العملية حيّة ومستمرة (تحقَّقتُ عبر PowerShell مباشرة `Test-Path` → `False`). هذا الملف يُكتَب مرة واحدة فقط عند بدء التشغيل (`acquireLock()`) ولا يُعاد كتابته كل دورة — فاختفاؤه أثناء التشغيل **لا يعني توقّف العملية**، فقط يعني أن فحص "هل تعمل؟" بالاعتماد على وجود هذا الملف وحده **غير موثوق بعد الآن**. **المرجع الأدق لسؤال "هل factory_loop حيّ؟" هو حداثة آخر سطر في `factory_loop.log` (يجب ألا يتجاوز عمره ~15 دقيقة أثناء التشغيل الطبيعي)، لا وجود ملف القفل.** لم يُحقَّق في سبب اختفاء الملف — يستحق نظرة إن تكرر.
- `NEEDS_ATTENTION.md` غائب (سليم). `python scripts/readiness_certificate.py` آخر تشغيل (18:54 UTC): **BLOCKED** — نفس السبب المعروف (3 مخالفات تسعير قديمة من قبل إصلاح ADR-010، لا تزال ضمن نافذة 24 ساعة، الساعة 14:13 تقريباً — مُتوقَّع أن تخرج من النافذة وتُغلَق تلقائياً).

## توصيتي كـPrincipal Architect: أول شيء تفعله الجلسة القادمة

**لا تبدأ ببناء كود جديد.** ابدأ بثلاث خطوات تشخيصية رخيصة، بهذا الترتيب:

1. **`python scripts/readiness_certificate.py`** — تحقّق من الحالة الحقيقية اليوم، لا افتراضاً من هذا الملف. إن تحوّلت `constitutional_pricing` إلى PASS (متوقَّع بعد ~14:13 غداً) وبقيت كل المعايير الأخرى PENDING فقط (لا FAIL)، هذا تقدّم حقيقي يستحق إخبار الرئيس به مباشرة دون انتظار طلبه.
2. **تحقّق من حداثة `factory_loop.log`** (آخر سطر، لا عمر `.factory_loop.lock` — انظر الملاحظة أعلاه) للتأكد أن الحلقة لا تزال حيّة قبل أي عمل آخر.
3. **اقرأ الوثائق الخمس المذكورة في البانر الأحمر أعلى هذا الملف** (الرؤية الموسّعة + الهوية) قبل قبول أي طلب بناء جديد — كلاهما يقيّدان أي قرار معماري الآن.

فقط بعد هذه الثلاث، إن طلب الرئيس شيئاً جديداً (خط إنتاج، تجزئة حسابات، تفعيل حي)، تكون البداية من أرضية حقيقية لا افتراضية.

## 3. القاعدتان الحرجتان اللتان لا تتغيّران بدون إذن صريح

**نشر (Gumroad):** لا نشر حقيقي حدث حتى الآن، وبنيوياً لا يمكن أن يحدث حتى يتوفر **كلا** الشرطين معاً:
- `GUMROAD_ACCESS_TOKEN` في `.env` (غير موجود اليوم)
- `FACTORY_LIVE_PUBLISH=true` (غير مضبوط اليوم)

**إنتاج (Golden Hunter → كتاب جديد):** لا إنتاج تلقائي حقيقي حدث حتى الآن، وبنيوياً لا يمكن أن يحدث حتى يُضبَط:
- `FACTORY_AUTO_PRODUCE=true` (غير مضبوط اليوم) — **راجع `AUTO_PRODUCE_ACTIVATION_CHECKLIST.md` قبل اقتراح تفعيله، وتأكَّد أن الرئيس راجع دورة `dry_run` كاملة أولاً.**

هذه أربعة حواجز مستقلة عبر طبقتين مختلفتين (الخادم + الأذرع للنشر، متغيّر بيئة منفصل للإنتاج). **لا تضِف أي توكن ولا تفعّل أي متغيّر من هذين دون طلب صريح من الرئيس.**

## 4. الفجوات المعروفة (مرتَّبة حسب الأهمية)

1. **لا استطلاع مبيعات فعلي** — `channels/ledger.py` يدعم أحداث `sale`، لكن لا شيء يستدعي `get_sales()` بشكل دوري بعد. `reality.py` لا يزال يعتمد على إدخال يدوي (`finance_data.json`) لأي رقم إيراد حقيقي. (أولوية #2 في `AUTOMATION_GAPS_REPORT.md` — لا قيمة له قبل وجود `GUMROAD_ACCESS_TOKEN`)
2. **`reality.py`'s `days_since_first_publish`** لا يزال KDP فقط — لا يحسب "أول نشر" عبر Gumroad/القنوات الأخرى.
3. **ذراع واحدة فقط مسجَّلة (`gumroad`)** — Payhip/Etsy/Redbubble متعمَّد تأجيلها (ADR-8): لا تُبنى قبل أن يبيع Gumroad دولاراً حقيقياً واحداً.
4. ~~لا تنبيه بشري~~ **✅ أُغلقت اليوم** — `NEEDS_ATTENTION.md` (`factory_loop.js`) يُنشَأ/يُحذَف تلقائياً عند 3 إشارات مشبوهة متتالية. 34 اختباراً.
5. **تنوّع مرشَّحي `market_hunter.py` قد يكون محدوداً** (اكتُشف أثناء بناء `ADR-011`) — نفس النيتش تقريباً تصدَّر `market_hunter_runs.log` في 3 أيام متتالية (09-07 إلى 11-07). لم يُحقَّق فيه بعد — يستحق النظر إن بقي معيار "3 نيتشات مختلفة" في شهادة الجاهزية معلَّقاً طويلاً.
6. **لم يُشغَّل اختبار حي كامل (توليد كتاب جديد فعلي عبر Groq → توزيع)** — كل التحقق تم بمسارات معزولة/مموَّهة لتفادي إنفاق رصيد Groq حقيقي بلا إذن. أول تفعيل حقيقي لـ `FACTORY_AUTO_PRODUCE` سيكون أول اختبار حقيقي كامل للحلقة بأسرها.

## 5. ملفات غير متعلقة بهذا العمل — لا تفترض أنها جزء منه

توجد تعديلات محلية غير محفوظة (uncommitted) على `economics.py`, `GOLDEN_OPPORTUNITIES.md`, `config/economics.json`, وغيرها، سابقة لهذه الجلسة ولم تُلمس. لا تفترض أنها مرتبطة بعمل الأذرع/التوزيع، ولا تُدرجها في أي commit خاص بهذا الموضوع دون التحقق من مصدرها أولاً.

## 6. الحدود التي بقيت سارية طوال هذا العمل

توقّف واسأل قبل: حذف ملف، تغيير schema يكسر بيانات قديمة، أو أي نشر حقيقي فعلي. لا سبب لتغيير هذه القاعدة الآن.
