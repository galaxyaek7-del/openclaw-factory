# 🐙 OCTOPUS ARCHITECTURE — وثيقة القرارات المعمارية

**المشروع:** OpenClaw Factory
**التاريخ:** 2026-07-11
**الحالة:** مُعتمَدة للتنفيذ التدريجي
**المبدأ الحاكم:** لا نلمس الكود العامل — نلفّه. لا ذراع ثانية قبل عمود فقري. لا بناء على بيانات تالفة.

> هذه وثيقة قرارات (ADR)، لا كود. الكود يأتي ملفاً ملفاً بعد اعتمادها.

---

## 0. الحقائق التي بُنيت عليها هذه الوثيقة (من الفحص الميداني)

| الحقيقة | المصدر | الأثر المعماري |
|---|---|---|
| `channels/gumroad_publisher.py` كامل: 4 عمليات CRUD تستدعي Gumroad API فعلياً | التقرير | **يُلفّ، لا يُعاد بناؤه** |
| الذراع **غير مختبَرة أبداً** (صفر استدعاء) | التقرير | تحتاج dry-run قبل أي رفع حقيقي |
| الاستدعاء **يدوي فقط** — لا استيراد في `server.js` ولا `factory_loop.js` | التقرير | **الذراع مفصولة عن الجسم — العمود الفقري مفقود** |
| **لا `interface` مشترك ولا نقطة تسجيل** لأذرع جديدة | التقرير | كل ذراع جديدة = نسخ يدوي هشّ → نحتاج عقداً موحّداً |
| خط الإنتاج ينتج `JSONL` **غير متوافق** مع ما يطلبه الناشر | التقرير | نحتاج طبقة ترجمة واحدة (schema) |
| `data/finance.json` **تالف** (يبدأ بـ `{{`) | التقرير | إصلاح قبل أي بناء يعتمد عليه |
| الأسرار: `GROQ_KEY` في `.env`، `GUMROAD_ACCESS_TOKEN` مطلوب وغائب | التقرير | كل ذراع تقرأ سرّها وتفشل بأمان إن غاب |

**الحفرة الكبرى الحقيقية:** ذراع كاملة معزولة عن الجسم، بلا عمود فقري يربط الأذرع، وبيانات مالية تالفة. المصنع عنده يد قوية غير موصولة بكتف.

---

## 1. الرؤية المعمارية: الأخطبوط

```
                    ┌─────────────────────────┐
                    │   الدماغ (موجود)         │
                    │ market_hunter → oracle   │
                    │ → inspectors → economics │
                    │ → book_generator         │
                    │ يُنتج: منتج مُتحقَّق منه  │
                    └───────────┬─────────────┘
                                │  (منتج JSONL)
                                ▼
                    ┌─────────────────────────┐
                    │  schemas/product.py      │ ◄── جديد: طبقة الترجمة
                    │  JSONL → Product موحّد    │     (تُصلح عدم التوافق)
                    └───────────┬─────────────┘
                                │  (Product canonical)
                                ▼
                    ┌─────────────────────────┐
                    │   distributor.py         │ ◄── جديد: العمود الفقري
                    │   العمود الفقري          │     يوزّع + يعزل الفشل
                    └──┬────────┬────────┬─────┘
                       │        │        │
              ┌────────▼──┐ ┌───▼────┐ ┌─▼──────────┐
              │ gumroad   │ │ payhip │ │ redbubble  │ ◄── أذرع (BaseArm)
              │ _arm.py   │ │ _arm   │ │ _arm       │
              │ (يلفّ     │ │(مستقبل)│ │ (مستقبل،   │
              │ الموجود)  │ │        │ │  هشّ، آخر) │
              └────┬──────┘ └────────┘ └────────────┘
                   │ يكتب النتيجة
                   ▼
              ┌──────────────────────┐
              │ data/sales_ledger    │ ◄── جديد: سجل حقيقة المبيعات
              │ .jsonl (append-only) │     reality.py يقرأ منه
              └──────────────────────┘
```

**ثلاث طبقات جديدة فقط، والباقي يبقى كما هو:**
1. **العقد** (`schemas/product.py` + `channels/base_arm.py`) — لغة موحّدة بين الدماغ والأذرع.
2. **العمود الفقري** (`distributor.py` + `channels/registry.py`) — يوزّع منتجاً واحداً على أذرع كثيرة، ويعزل فشل كل ذراع.
3. **السجل** (`data/sales_ledger.jsonl`) — مصدر الحقيقة للمبيعات، يستبدل الكذبة `published: true`.

---

## 2. Executive Review — 10 أبعاد

| البُعد | التقييم | القرار المترتب |
|---|---|---|
| **استراتيجي** | الأذرع المعزولة = يد واحدة. العمود الفقري يحوّل المصنع من ناشر أحادي إلى موزّع متعدد | العمود الفقري أولوية قصوى، قبل أي ذراع جديدة |
| **تجاري** | Gumroad جاهز 95% → أقصر طريق لأول دولار آلي حقيقي | نُفعّل Gumroad أولاً، لا نبدأ من الصفر |
| **مالي** | `finance.json` تالف = المصنع أعمى مالياً | إصلاح + `sales_ledger` قبل أي توسّع |
| **تقني** | لا عقد مشترك = ديْن تقني ينمو مع كل ذراع | `BaseArm` ABC يُثبّت العقد مرة واحدة |
| **صيانة** | نسخ يدوي لكل ذراع = كابوس صيانة | Registry: ذراع جديدة = ملف + سطر تسجيل |
| **أمان** | سرّ غائب يجب ألا يُسقط النظام | كل ذراع تفشل بأمان (fail-safe مثل safety_filter) |
| **موثوقية** | فشل ذراع يجب ألا يُسقط الأخريات | كل رفع داخل try/catch معزول + circuit breaker |
| **قابلية التوسّع** | 5 أذرع مستقبلاً على منتج واحد | distributor يوزّع بالتوازي على قائمة أذرع مسجّلة |
| **أتمتة** | الاستدعاء يدوي = ليس مصنعاً | العمود الفقري يُستدعى من `factory_loop.js` تلقائياً (المرحلة الأخيرة) |
| **قابلية الامتداد** | إضافة منصة يجب ألا تلمس النواة | مبدأ Open/Closed: النواة مغلقة للتعديل، مفتوحة للامتداد |

---

## 3. عقد الواجهة المشترك (BaseArm)

كل ذراع — Gumroad أو Payhip أو Redbubble — تلتزم بنفس العقد. هذا يمنع الفوضى ويجعل العمود الفقري لا يعرف ولا يهتم بأي منصة يكلّم.

```python
# channels/base_arm.py  (جديد)
from abc import ABC, abstractmethod
from dataclasses import dataclass
from enum import Enum

class ArmStatus(Enum):
    READY = "ready"              # سرّ موجود، جاهزة
    UNAVAILABLE = "unavailable"  # سرّ غائب — تُتخطّى بأمان
    COOLDOWN = "cooldown"        # قاطع الدائرة فتح بعد فشل متكرر

@dataclass
class PublishResult:
    ok: bool
    platform: str
    product_id: str | None      # id المنتج على المنصة
    url: str | None             # رابط البيع
    error: str | None
    dry_run: bool

class BaseArm(ABC):
    name: str                   # "gumroad" ...

    @abstractmethod
    def status(self) -> ArmStatus:
        """تتحقق من السرّ/الجاهزية. لا سرّ = UNAVAILABLE، لا استثناء."""

    @abstractmethod
    def supports(self, product) -> bool:
        """هل هذه الذراع تصلح لهذا المنتج؟ (مثلاً: رقمي فقط)"""

    @abstractmethod
    def publish(self, product, dry_run: bool = True) -> PublishResult:
        """ترفع المنتج. dry_run=True افتراضياً: تطبع ولا ترفع."""
```

**لماذا `dry_run=True` افتراضياً؟** دفاعياً: لا رفع حقيقي إلا بطلب صريح. الافتراضي الآمن يمنع حوادث النشر.

---

## 4. المنتج الموحّد (Canonical Product)

يُصلح عدم التوافق بين JSONL والناشر في **مكان واحد**، لا في كل ذراع.

```python
# schemas/product.py  (جديد)
from dataclasses import dataclass

@dataclass
class Product:
    title: str
    subtitle: str
    description: str
    price_usd: float
    file_path: str        # مسار الـ PDF
    cover_path: str | None  # قد يكون None — الأذرع تقرر
    tags: list[str]
    language: str         # "ar" | "en"
    source_id: str        # id السجل الأصلي في JSONL

    @staticmethod
    def from_jsonl_record(record: dict) -> "Product":
        """الجسر: سجل خط الإنتاج → منتج موحّد. هنا تُعالَج كل اختلافات الصيغة."""
        ...
```

**قرار الغلاف الناقص (`cover_path: None`):** لا نمنع الرفع. كل ذراع تقرر: Gumroad يقبل بلا غلاف (تحذير فقط)، منصات أخرى قد ترفض. القرار داخل الذراع، لا في العمود الفقري.

---

## 5. القرارات المعمارية (ADR)

**ADR-1 — لغة العمود الفقري: Python.**
`distributor.py` بايثون، يستدعيه Node عبر `subprocess` (نفس نمط `book_generator` الموجود في `server.js`). السبب: عملاء API للأذرع بايثون أصلاً؛ نتجنّب إعادة الكتابة؛ درز واحد نظيف بين الجسم والأذرع.
*البديل المرفوض:* منسّق Node يستدعي كل ذراع منفصلة — دروز أكثر، معالجة أخطاء مكرّرة.

**ADR-2 — لفّ لا إعادة بناء.**
`gumroad_arm.py` محوّل رفيع (adapter) يلفّ `gumroad_publisher.py` الموجود ويطبّق `BaseArm`. **لا نلمس الناشر العامل إطلاقاً.**

**ADR-3 — العقد قبل الأذرع.**
`BaseArm` + `Product` يُثبّتان أولاً. بلا عقد، كل ذراع تعيد اختراع الفوضى.

**ADR-4 — Registry للتوسّع.**
`channels/registry.py`: ذراع جديدة = ملف جديد + سطر `register(MyArm())`. النواة (`distributor`) لا تتغيّر أبداً. مبدأ Open/Closed.

**ADR-5 — عزل الفشل + قاطع دائرة لكل ذراع.**
كل `publish` داخل try/catch مستقل. فشل ذراع يُسجَّل ولا يُسقط الأخريات. بعد فشل متكرر → الذراع تدخل COOLDOWN (نعيد استخدام مفهوم قاطع الدائرة الموجود في `factory_loop.js`).

**ADR-6 — أسرار fail-safe.**
كل ذراع تفحص سرّها في `status()`. سرّ غائب = `UNAVAILABLE`، تُتخطّى بهدوء. لا استثناء يُسقط النظام. نفس فلسفة `safety_filter.py`.

**ADR-7 — إصلاح طبقة البيانات.**
إصلاح `finance.json` التالف (`{{`). إضافة `data/sales_ledger.jsonl` (append-only) كمصدر حقيقة للمبيعات. `reality.py` يقرأ منه بدل الكذبة `published: true`.

**ADR-8 — ترتيب الأذرع (بالأدلة، لا الحماس):**

| المرحلة | الذراع | لماذا هنا | الهشاشة |
|---|---|---|---|
| **0** | لا ذراع — الأساس | schema + BaseArm + registry + distributor هيكل + إصلاح finance.json | — |
| **1** | 🥇 Gumroad | كامل، API رسمي، رقمي، جاهز 95% → أول دولار | منخفضة |
| **2** | 🥈 Payhip | API رسمي، رسوم أقل، **يُثبت أن التوسّع يعمل** (ذراع بالإسقاط) | منخفضة |
| **3** | 🟡 Etsy | API يحتاج موافقة تطبيق → احتكاك | متوسطة |
| **4** | ⚠️ Redbubble | **لا API** — Puppeteer/Node، خطر حظر ToS → **معزول، موثّق، أخيراً** | عالية |

**قرار Redbubble صريح:** بلا API رسمي، يخالف نمط الأذرع البايثونية، ويحمل خطر إغلاق الحساب. يبقى **آخر ذراع**، معزولاً في مسار Node منفصل، مع تحذير مكتوب في `LESSONS_LEARNED.md`. لا يُبنى قبل أن تُثبت الأذرع الرسمية أنها تبيع.

---

## 6. خريطة الملفات النهائية

```
openclaw-dasgboard/
├── schemas/
│   └── product.py            ← جديد: العقد (Canonical Product + JSONL bridge)
├── channels/
│   ├── base_arm.py           ← جديد: عقد الواجهة (BaseArm ABC)
│   ├── registry.py           ← جديد: تسجيل الأذرع
│   ├── gumroad_publisher.py  ← موجود: لا يُلمس
│   ├── gumroad_arm.py        ← جديد: محوّل يلفّ الناشر
│   └── payhip_arm.py         ← مستقبل (المرحلة 2)
├── distributor.py            ← جديد: العمود الفقري
├── data/
│   ├── finance.json          ← إصلاح (كان تالفاً)
│   └── sales_ledger.jsonl    ← جديد: سجل المبيعات الحقيقي
├── server.js                 ← تعديل لاحق: endpoint /api/distribute
└── factory_loop.js           ← تعديل أخير: استدعاء آلي
```

---

## 7. جرد الحفر (Hole Inventory) — صادق قبل التنفيذ

1. 🕳️ **الذراع مفصولة عن الجسم** — لا عمود فقري. (يُغلق بالمرحلة 0-1)
2. 🕳️ **لا عقد مشترك** — كل ذراع تعيد الاختراع. (يُغلق بـ BaseArm)
3. 🕳️ **`finance.json` تالف** — عمى مالي. (يُغلق بـ ADR-7)
4. 🕳️ **`published: true` كذبة** — المصنع يخدع نفسه. (يُستبدل بـ sales_ledger)
5. 🕳️ **الذراع غير مختبَرة** — صفر استدعاء لـ Gumroad API. (يُغلق بـ dry-run المرحلة 1)
6. 🕳️ **عدم توافق JSONL/الناشر** — (يُغلق بطبقة Product)
7. 🕳️ **غلاف null** — قرار مؤجّل داخل كل ذراع. (موثّق، غير حاجب)

---

## 8. ما لا نبنيه الآن عمداً (حارس YAGNI)

- ❌ Redbubble/Etsy قبل أن تبيع Gumroad دولاراً واحداً.
- ❌ لوحة تحكم فاخرة للأذرع — سطر أمر يكفي.
- ❌ نشر متوازي متعدد قبل أن يعمل نشر أحادي.
- ❌ Telegram bot، recycler — خارج نطاق الأذرع.

---

## 9. أول ملف عند الانطلاق

**`schemas/product.py` + `channels/base_arm.py`** — العقد أولاً. بلا لغة مشتركة، كل ما بعده يُبنى على رمل. ملف واحد، مُختبَر، ثم التالي.

**قاعدة التنفيذ:** خطوة واحدة، ملف واحد، اختبار، ثم التالي. نسخة احتياطية (backup branch) قبل لمس أي ملف حيّ. لا `git add -A`. كل حفرة تُغلق تُوثّق في `LESSONS_LEARNED.md`.

---

## 10. إضافة — قرارات مراجعة كبير المهندسين (2026-07-11)

قبل التنفيذ، طرح المدير التنفيذي هذه الوثيقة على كبير المهندسين للمراجعة التقنية. اكتُشف أثناء المراجعة أن خطة تنفيذ ثانية كانت مقترحة بالتوازي (`arms/`, `data/published_events.jsonl`) تتعارض جزئياً مع هذه الوثيقة. القرارات التالية تحسم التعارض وتُعتمد رسمياً فوق كل ما سبق:

1. **التسمية:** `channels/` فقط. لا مجلد `arms/` يُبنى. هذه الوثيقة (§6 خريطة الملفات) هي المرجع.
2. **اللغة:** بايثون + `subprocess`، مطابقة لنمط `book_generator.py` الحالي في `server.js` (ADR-1 يبقى كما هو).
3. **لا queue، لا async في هذه المرحلة.** كل الأذرع والعمود الفقري synchronous. يُعاد النظر فقط عند وجود أكثر من ذراع رسمية تعمل فعلاً بالتوازي.
4. **سجل واحد موحّد:** `data/sales_ledger.jsonl` (وليس ملف منفصل لـ"أحداث النشر"). يحوي نوعي سجل: محاولة نشر (`publish_attempt`) ومبيعة حقيقية (`sale`, عبر `get_sales`) — لا ملف ثالث بجانب `finance_data.json` و`data/finance.json`.
5. **إصلاح `data/finance.json` يسبق أي بناء آخر** — تنفيذاً حرفياً لـ ADR-7.
6. **هذه الوثيقة (`OCTOPUS_ARCHITECTURE.md`) هي المرجع المعتمد الوحيد.** أي مستند أو خطة أخرى تتعارض معها تُحسم لصالحها.

**ترتيب التنفيذ المعتمد (8 خطوات):**
1. هذه الإضافة (توثيق القرارات).
2. إصلاح `data/finance.json`.
3. `channels/base_arm.py` + توسيع `schemas/product.py` الموجود (لا استبداله).
4. `channels/registry.py` + `PROGRESS_REPORT.md` أول تقرير.
5. `channels/gumroad_arm.py` كمحوّل رقيق فوق `gumroad_publisher.py` (لا يُلمس).
6. `tests/test_base_arm.py` — يختبر العقد وdry_run فقط، لا API حياً.
7. `data/sales_ledger.jsonl` — كاتب موحّد.
8. مسار توزيع dry-run + تحديث `PROGRESS_REPORT.md`. **لا ربط بنشر Gumroad حقيقي دون إذن صريح.**

---

## 11. إضافة — الرؤية الموسّعة (2026-07-11، توجيه استراتيجي رسمي من الرئيس)

> تحديث بموجب طلب صريح من الرئيس عبد القادر لتعديل هذه الوثيقة تحديداً — استثناء واعٍ من قاعدة "توقّف واسأل قبل تغيير `OCTOPUS_ARCHITECTURE.md`" في `PRINCIPAL_ARCHITECT_CHARTER.md` §3، لا تجاوزاً صامتاً لها. التفاصيل الكاملة في `PRODUCT_VISION.md`، `ADR-012`، `ADR-013`.

**§1 (الرؤية المعمارية: الأخطبوط) تُقرَأ من الآن فصاعداً بامتداد صريح:** "الدماغ" و"طبقة الترجمة" و"العمود الفقري" و"الأذرع" في المخطط الأصلي وُصفت بمصطلحات كتاب (`book_generator`, JSONL كتب) لأن الكتب كانت خط الإنتاج الوحيد وقت كتابتها. **هذا لم يعد صحيحاً كافتراض دائم:** الكتب هي خط إنتاج أول من عشرة مخطَّطة (`PRODUCT_VISION.md` §2) — العمود الفقري (`distributor.py`) وطبقة الترجمة (`schemas/product.py`) والأذرع (`channels/`) صُمِّمت أصلاً بعمومية كافية لتستوعب هذا (`ADR-013` يُثبت أن عقد `BaseArm` نفسه محايد فعلاً)، لكن طبقة الترجمة الفعلية (`Product.from_jsonl_record`) وحقولها لا تزال مكتوبة لكتاب حرفياً (`ADR-012` يوثّق كل نقطة).

**§4 (المنتج الموحّد) — القرار الأصلي "لا نمنع الرفع" بسبب غلاف ناقص يبقى كما هو، لكن يُضاف إليه:** المنتج المُوحَّد (`Product`) وُصِف وقتها كـ"جسر JSONL → منتج موحّد" بافتراض ضمني أن كل منتج = ملف واحد + سعر واحد لمرة واحدة. هذا الافتراض **لم يعد كافياً** لخطوط الاشتراك (SaaS) أو الترخيص المخطَّطة — `ADR-012` يقترح حقولاً إضافية (`pricing_model`, `recurring_interval`) بافتراضات آمنة لا تكسر أي كود قائم، دون تنفيذها اليوم.

**§8 (ما لا نبنيه الآن عمداً) يبقى سارياً حرفياً ولا يتغيّر:** التوسّع لخطوط إنتاج جديدة يخضع لنفس حارس YAGN الذي منع Payhip/Etsy قبل إثبات Gumroad — **لا خط إنتاج جديد يُبنى قبل أن تُثبت الكتب دولاراً حقيقياً واحداً على الأقل**، ثم القرار للرئيس، لا لجدول زمني تقني.

**لا تغيير على أي قسم آخر (§1-9 الأصلية) — هذه إضافة تفسيرية، لا إعادة كتابة.**
